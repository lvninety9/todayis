---
phase: 17-템플릿-시스템-전면-개편
plan: 17-02
wave: 2
requirements:
  - TEMPLATE-DESIGN-02
status: complete
---

# Plan 17-02: 템플릿 미리보기 기능 구현 (TEMPLATE-DESIGN-02)

## Tasks

### Task 1: TemplateCard 클릭 → fullscreen preview modal

**Files to Modify:**
- `src/components/templates/library/TemplateCard.tsx` — 클릭 이벤트 → modal 열기
- `src/components/templates/preview/TemplatePreviewModal.tsx` — 새 fullscreen preview modal 컴포넌트
- `src/app/(main)/templates/page.tsx` — TemplateCard에 modal state 전달

**Implementation:**
1. TemplateCard에 `onPreview` prop 추가
2. 카드 클릭 시 `onPreview(template)` 호출
3. templates/page.tsx에서 preview modal state 관리
4. TemplatePreviewModal 컴포넌트 생성:
   - `backdrop-blur-md` 배경 + 반투명 overlay
   - `×` 버튼으로 닫기
   - 모바일: 화면 가득 (100vw × 100vh)
   - 데스크톱: max-width 600px, 중앙 정렬
   - TemplateEngine으로 렌더링 (빈 데이터로 미리보기)

### Task 2: PreviewModal — 템플릿 데이터 로드 + 렌더링

**Files to Modify:**
- `src/components/templates/preview/TemplatePreviewModal.tsx` — 데이터 로드 + 렌더링
- `src/types/template.ts` — Section 타입 정의 추가 (선행)

**Implementation:**
1. PreviewModal props: `template`, `isOpen`, `onClose`
2. template.fields 기반으로 빈 데이터 생성
3. TemplateEngine으로 렌더링 (Section 기반 호환)
4. 닫기 이벤트:
   - `×` 버튼 클릭
   - backdrop 클릭
   - Escape 키
5. 모달 열림/닫힘 애니메이션:
   - 열림: `fade-in + scale(0.95 → 1)`
   - 닫힘: `fade-out + scale(1 → 0.95)`

### Task 3: 템플릿 상세 페이지 → preview modal로 통합

**Files to Modify:**
- `src/app/(main)/templates/[id]/page.tsx` — modal 기반 레이아웃으로 변경
- `src/components/templates/preview/TemplatePreviewModal.tsx` — edit 모드 지원 추가

**Implementation:**
1. 템플릿 상세 페이지에서 preview modal 사용
2. "편집하기" 버튼 → preview-edit 모드 진입
3. 기존 [id]/edit 페이지는 유지 (하위 호환)
4. 새 경로: `/templates/[id]` → fullscreen preview

---

## Verification Criteria

- [ ] TemplateCard 클릭 → fullscreen preview modal 열림
- [ ] Modal에서 × 버튼으로 종료 가능
- [ ] backdrop 클릭으로 modal 닫힘
- [ ] Escape 키로 modal 닫힘
- [ ] 모바일에서 화면 가득 표시
- [ ] 데스크톱에서 중앙 정렬, max-width 600px
- [ ] 템플릿 상세 페이지 → preview modal로 통합

---

## Commits

- `feat(17-02): add fullscreen preview modal for template cards`
- `feat(17-02): add preview modal with backdrop blur and close handlers`
- `feat(17-02): integrate template detail page with preview modal`
