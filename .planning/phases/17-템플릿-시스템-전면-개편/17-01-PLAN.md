---
phase: 17-템플릿-시스템-전면-개편
plan: 17-01
wave: 1
requirements:
  - TEMPLATE-DESIGN-01
status: planned
---

# Plan 17-01: 템플릿 Visual Design 시스템 구축

## Goal

Wedding 템플릿의 3가지 visual style (romantic, classic, modern)을 정의하고, section 기반 렌더링 엔진의 기반을 구축. CSS variables, animation classes, Section 타입 정의를 포함.

## Requirements

- **TEMPLATE-DESIGN-01**: 템플릿 Visual Design — Wedding 전문 card 스타일 (romantic, classic, modern 등)

## Tasks

### Task 1: Section 타입 정의 (기반 아키텍처)

**Files to Create/Modify:**
- `src/types/template.ts` — Section 인터페이스 추가

**Implementation:**
1. Section 타입 정의 추가:
```typescript
export type SectionType = 'image' | 'announcement' | 'invitation' | 'map' | 'accounts';

export interface SectionStyle {
  animation?: 'none' | 'fade-in' | 'slide-up' | 'slide-left' | 'slide-right' | 'bounce' | 'scale-up';
  animationDuration?: number;
  animationDelay?: number;
  backgroundColor?: string;
  textColor?: string;
  accentColor?: string;
  borderColor?: string;
  fontFamily?: string;
  fontSize?: 'xs' | 'sm' | 'base' | 'lg' | 'xl' | '2xl';
}

export interface Section {
  id: string;
  type: SectionType;
  order: number;
  label: string;
  fields: TemplateField[];
  style?: SectionStyle;
}
```

2. Template 인터페이스에 `sections` 필드 추가 (기존 `fields`는 하위 호환 유지):
```typescript
export interface Template {
  // ... 기존 필드 유지
  sections?: Section[];  // section 기반 구조 (신규)
}
```

3. Section ID 생성 헬퍼 함수:
```typescript
export function generateSectionId(type: SectionType): string {
  return `${type}-${crypto.randomUUID().slice(0, 8)}`;
}
```

### Task 2: 3가지 Wedding 템플릿 스타일 정의

**Files to Create/Modify:**
- `src/app/globals.css` — 스타일별 CSS variables 추가
- `src/lib/template-styles.ts` — 새 파일 (스타일 정의)

**Implementation:**

1. `src/lib/template-styles.ts` 생성 — 스타일 정의 파일:
```typescript
export type TemplateStyle = 'romantic' | 'classic' | 'modern';

export interface TemplateStyleConfig {
  name: string;
  description: string;
  cssVariables: Record<string, string>;
  fonts: {
    heading: string;
    body: string;
  };
  colors: {
    primary: string;
    secondary: string;
    accent: string;
    background: string;
    text: string;
    border: string;
  };
}

export const templateStyles: Record<TemplateStyle, TemplateStyleConfig> = {
  romantic: {
    name: 'Romantic',
    description: '로맨틱한 느낌 — rose, pink, blush 톤',
    cssVariables: {
      '--style-primary': '350 70% 60%',      // blush/pink
      '--style-secondary': '30 80% 70%',      // peach
      '--style-accent': '300 50% 70%',        // lavender
      '--style-background': '30 20% 97%',     // warm white
      '--style-text': '350 40% 30%',          // deep rose
      '--style-border': '350 50% 80%',        // light pink
    },
    fonts: { heading: 'Playfair Display', body: 'Noto Serif KR' },
    colors: {
      primary: '#D4788F', secondary: '#F4A0B5', accent: '#C8A8D4',
      background: '#FFF9F9', text: '#5A3E4B', border: '#F4D4DC',
    },
  },
  classic: {
    name: 'Classic',
    description: '클래식한 느낌 — ivory, gold, black 톤',
    cssVariables: {
      '--style-primary': '45 70% 50%',        // gold
      '--style-secondary': '40 30% 85%',      // ivory
      '--style-accent': '30 10% 25%',         // dark brown
      '--style-background': '40 20% 96%',     // cream
      '--style-text': '30 10% 20%',           // dark
      '--style-border': '45 50% 75%',         // light gold
    },
    fonts: { heading: 'Playfair Display', body: 'Noto Serif KR' },
    colors: {
      primary: '#C9A96E', secondary: '#F5E6C8', accent: '#3D3B34',
      background: '#FDF8F0', text: '#2C2825', border: '#E8D5B0',
    },
  },
  modern: {
    name: 'Modern',
    description: '모던한 느낌 — minimalist, clean lines',
    cssVariables: {
      '--style-primary': '220 60% 50%',       // blue-gray
      '--style-secondary': '220 20% 90%',     // light gray
      '--style-accent': '160 40% 45%',        // sage
      '--style-background': '0 0% 98%',       // near white
      '--style-text': '220 20% 20%',          // dark gray
      '--style-border': '220 15% 85%',        // light gray
    },
    fonts: { heading: 'Pretendard', body: 'Pretendard' },
    colors: {
      primary: '#5B7FA5', secondary: '#E8ECF0', accent: '#81B29A',
      background: '#FAFAFA', text: '#2D3748', border: '#D4D8DC',
    },
  },
};

export function applyTemplateStyle(style: TemplateStyle, element: HTMLElement): void {
  const config = templateStyles[style];
  if (!config) return;
  Object.entries(config.cssVariables).forEach(([key, value]) => {
    element.style.setProperty(key, value);
  });
}
```

2. `globals.css`에 스타일별 CSS class 추가:
```css
/* Template style classes */
.template-style-romantic {
  --style-primary: 350 70% 60%;
  --style-secondary: 30 80% 70%;
  --style-accent: 300 50% 70%;
  --style-background: 30 20% 97%;
  --style-text: 350 40% 30%;
  --style-border: 350 50% 80%;
  font-family: 'Noto Serif KR', serif;
}

.template-style-classic {
  --style-primary: 45 70% 50%;
  --style-secondary: 40 30% 85%;
  --style-accent: 30 10% 25%;
  --style-background: 40 20% 96%;
  --style-text: 30 10% 20%;
  --style-border: 45 50% 75%;
  font-family: 'Noto Serif KR', serif;
}

.template-style-modern {
  --style-primary: 220 60% 50%;
  --style-secondary: 220 20% 90%;
  --style-accent: 160 40% 45%;
  --style-background: 0 0% 98%;
  --style-text: 220 20% 20%;
  --style-border: 220 15% 85%;
  font-family: 'Pretendard', sans-serif;
}
```

### Task 3: Animation CSS classes 추가

**Files to Modify:**
- `src/app/globals.css` — animation classes 추가

**Implementation:**

1. Section 레벨 애니메이션 classes 추가:
```css
/* Section animation classes */
@keyframes animate-fade-in {
  from { opacity: 0; }
  to { opacity: 1; }
}

@keyframes animate-slide-up {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}

@keyframes animate-slide-left {
  from { opacity: 0; transform: translateX(20px); }
  to { opacity: 1; transform: translateX(0); }
}

@keyframes animate-slide-right {
  from { opacity: 0; transform: translateX(-20px); }
  to { opacity: 1; transform: translateX(0); }
}

@keyframes animate-bounce {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-8px); }
}

@keyframes animate-scale-up {
  from { opacity: 0; transform: scale(0.95); }
  to { opacity: 1; transform: scale(1); }
}

.animate-fade-in { animation: animate-fade-in 0.6s ease-out both; }
.animate-slide-up { animation: animate-slide-up 0.6s ease-out both; }
.animate-slide-left { animation: animate-slide-left 0.6s ease-out both; }
.animate-slide-right { animation: animate-slide-right 0.6s ease-out both; }
.animate-bounce { animation: animate-bounce 0.8s ease-out 1; }
.animate-scale-up { animation: animate-scale-up 0.5s ease-out both; }

/* Animation duration variants */
.animate-duration-fast { animation-duration: 0.3s; }
.animate-duration-normal { animation-duration: 0.6s; }
.animate-duration-slow { animation-duration: 1.0s; }
```

2. `globals.css`에 Google Fonts import 추가:
```css
@import url('https://fonts.googleapis.com/css2?family=Noto+Serif+KR:wght@400;700&family=Playfair+Display:wght@400;700&family=Pretendard:wght@400;700&display=swap');
```

### Task 4: TemplateEngine — section 기반 렌더링 (기반)

**Files to Modify:**
- `src/components/templates/engine/TemplateEngine.tsx` — section 기반 렌더링 지원

**Implementation:**

1. 기존 flat field 렌더링 유지 + section 기반 렌더링 추가:
```typescript
interface TemplateEngineProps {
  template: Template;
  data: TemplateData;
  style?: TemplateStyle;
}

export function TemplateEngine({ template, data, style }: TemplateEngineProps) {
  // section 기반 렌더링 (sections가 있으면 우선 사용)
  if (template.sections && template.sections.length > 0) {
    return <SectionBasedRenderer sections={template.sections} data={data} style={style} />;
  }
  // 기존 flat field 렌더링 (하위 호환)
  return <FlatFieldRenderer template={template} data={data} />;
}
```

2. `SectionBasedRenderer` 컴포넌트 추가:
```typescript
function SectionBasedRenderer({ sections, data, style }: {
  sections: Section[];
  data: TemplateData;
  style?: TemplateStyle;
}) {
  const sortedSections = [...sections].sort((a, b) => a.order - b.order);
  
  return (
    <div className={cn(
      'template-preview-sections',
      style && `template-style-${style}`
    )}>
      {sortedSections.map((section) => (
        <SectionRenderer
          key={section.id}
          section={section}
          data={data}
        />
      ))}
    </div>
  );
}
```

3. `SectionRenderer` 컴포넌트 — 각 section 타입별 렌더링:
```typescript
function SectionRenderer({ section, data }: {
  section: Section;
  data: TemplateData;
}) {
  const { type, fields, style } = section;
  
  // 애니메이션 class 적용
  const animationClass = style?.animation && style.animation !== 'none'
    ? `animate-${style.animation}`
    : '';
  
  const sectionStyle: React.CSSProperties = {
    ...(style?.backgroundColor && { backgroundColor: style.backgroundColor }),
    ...(style?.textColor && { color: style.textColor }),
    ...(style?.fontFamily && { fontFamily: style.fontFamily }),
  };
  
  switch (type) {
    case 'image':
      return <ImageSection fields={fields} data={data} style={sectionStyle} className={animationClass} />;
    case 'announcement':
      return <AnnouncementSection fields={fields} data={data} style={sectionStyle} className={animationClass} />;
    case 'invitation':
      return <InvitationSection fields={fields} data={data} style={sectionStyle} className={animationClass} />;
    case 'map':
      return <MapSection fields={fields} data={data} style={sectionStyle} className={animationClass} />;
    case 'accounts':
      return <AccountsSection fields={fields} data={data} style={sectionStyle} className={animationClass} />;
    default:
      return null;
  }
}
```

4. 각 section 타입별 렌더링 컴포넌트 구현:
   - `ImageSection`: 메인 이미지 표시
   - `AnnouncementSection`: "결혼합니다" 멘트 표시
   - `InvitationSection`: 초대 문구, 날짜, 장소 표시
   - `MapSection`: 지도 표시 (Google Maps 링크)
   - `AccountsSection`: 계좌번호 표시 (신랑측/신부측)

## Verification Criteria

- [ ] Section 인터페이스가 src/types/template.ts에 정의됨
- [ ] Template 인터페이스에 sections 필드 추가됨
- [ ] 3가지 스타일 (romantic, classic, modern)이 src/lib/template-styles.ts에 정의됨
- [ ] globals.css에 스타일별 CSS class (.template-style-romantic 등) 추가됨
- [ ] globals.css에 animation classes (fade-in, slide-up, bounce 등) 추가됨
- [ ] TemplateEngine이 section 기반 렌더링 지원 (sections가 있으면 우선 사용)
- [ ] 기존 flat field 렌더링이 하위 호환으로 유지됨

## Commits

- `feat(17-01): add Section type definition with style configuration`
- `feat(17-01): add 3 wedding template styles (romantic, classic, modern)`
- `feat(17-01): add section animation CSS classes`
- `feat(17-01): add section-based rendering to TemplateEngine`
