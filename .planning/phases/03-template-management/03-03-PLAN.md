---
phase: 03-template-management
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified: [src/app/(main)/templates/page.tsx, src/components/templates/library/TemplateLibrary.tsx, src/components/templates/library/TemplateCard.tsx, src/components/templates/library/TemplateUploadDialog.tsx]
autonomous: false
requirements: [TEMPLATE-MANAGE-02, TEMPLATE-MANAGE-03]
user_setup: []

must_haves:
  truths:
    - User can view all templates in grid layout
    - User can filter templates by category
    - User can search templates by name
    - User can create new template via upload dialog
    - User can edit template details
    - User can delete templates with confirmation
  artifacts:
    - path: src/app/(main)/templates/page.tsx
      provides: "Templates page layout and state management"
      min_lines: 80
    - path: src/components/templates/library/TemplateLibrary.tsx
      provides: "Main library component with grid layout"
      min_lines: 100
    - path: src/components/templates/library/TemplateCard.tsx
      provides: "Individual template card component"
      min_lines: 50
    - path: src/components/templates/library/TemplateUploadDialog.tsx
      provides: "Template creation/upload dialog"
      min_lines: 60
  key_links:
    - from: src/app/(main)/templates/page.tsx
      to: src/components/templates/library/TemplateLibrary.tsx
      via: "component import"
      pattern: "import.*TemplateLibrary"
    - from: src/components/templates/library/TemplateLibrary.tsx
      to: src/app/api/templates/route.ts
      via: "API fetch"
      pattern: "fetch.*\\/api\\/templates"

---

<objective>
Implement template library UI for browsing and managing templates

Purpose: Provide intuitive interface for template management

Output: Template library page with CRUD operations
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-template-management/03-01-SUMMARY.md
@.planning/phases/03-template-management/03-02-SUMMARY.md

# Existing Code Context
@src/app/(main)/dashboard/page.tsx
@src/components/templates/preview/TemplatePreview.tsx
@src/components/templates/editor/TemplateEditor.tsx
@src/hooks/use-session.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From src/types/template.ts:
```typescript
export interface Template {
  id: string;
  userId: string;
  name: string;
  category: string;
  thumbnail: string;
  fields: TemplateField[];
  layout: string;
  isPublished: boolean;
  downloadCount: number;
  createdAt: string;
  updatedAt: string;
}
```

From src/app/(main)/dashboard/page.tsx:
```typescript
'use client';
import { useSession } from '@/hooks/use-session';
import { useRouter } from 'next/navigation';
import { useEffect } from 'react';
```

From src/components/templates/preview/TemplatePreview.tsx:
```typescript
interface TemplatePreviewProps {
  template: Template;
  mode?: 'view' | 'edit';
  onEdit?: (template: Template) => void;
}
```
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TemplateCard component</name>
  <files>src/components/templates/library/TemplateCard.tsx</files>
  <action>
    Create reusable template card component:
    
    Props:
    - template: Template (required)
    - mode: 'view' | 'edit' (default: 'view')
    - onSelect: (template: Template) => void (for edit mode)
    - onDelete: (template: Template) => void (for edit mode)
    
    Display:
    - Thumbnail image (from template.thumbnail)
    - Template name (truncate if too long)
    - Category badge
    - Download count badge
    - Published status indicator
    
    Actions (edit mode):
    - Edit button -> calls onSelect
    - Delete button -> calls onDelete with confirmation
    
    Styling:
    - Use shadcn/ui Card component
    - Hover effects on card
    - Responsive grid layout (1 col mobile, 2 col tablet, 3-4 col desktop)
    - Consistent with dashboard page styling
    
    Reference Phase 1 TemplatePreview for thumbnail rendering.
    Use shadcn/ui Button, Badge components.
  </action>
  <verify>
    <automated>cd /media/jay/D/cursor/todayis && npx tsc --noEmit src/components/templates/library/TemplateCard.tsx</automated>
  </verify>
  <done>
    TemplateCard component created
    Displays template info with thumbnail
    Edit mode shows action buttons
    Responsive grid layout
    TypeScript compilation passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Create TemplateLibrary component</name>
  <files>src/components/templates/library/TemplateLibrary.tsx</files>
  <action>
    Create main library component with grid layout:
    
    Props:
    - templates: Template[] (required)
    - mode: 'view' | 'edit' (default: 'view')
    - onSelect: (template: Template) => void
    - onDelete: (template: Template) => void
    - onCreate: () => void (opens upload dialog)
    - onFilter: (category: string) => void
    - onSearch: (query: string) => void
    
    Features:
    - Grid layout using TemplateCard components
    - Category filter dropdown (all, wedding, birthday, custom)
    - Search input for template name
    - Empty state message when no templates
    - Loading state with skeleton cards
    - Sort by: name, date, downloads
    
    State:
    - Current filter category
    - Search query
    - Sort order
    
    Styling:
    - Consistent with dashboard page
    - Use shadcn/ui Select, Input components
    - Responsive grid (1-4 columns)
    
    Reference Phase 2 dashboard for layout patterns.
  </action>
  <verify>
    <automated>cd /media/jay/D/cursor/todayis && npx tsc --noEmit src/components/templates/library/TemplateLibrary.tsx</automated>
  </verify>
  <done>
    TemplateLibrary component created
    Grid layout with TemplateCard components
    Category filter and search functionality
    Loading and empty states
    TypeScript compilation passes
  </done>
</task>

<task type="auto">
  <name>Task 3: Create TemplateUploadDialog component</name>
  <files>src/components/templates/library/TemplateUploadDialog.tsx</files>
  <action>
    Create dialog for creating new template:
    
    Props:
    - open: boolean (controlled)
    - onOpenChange: (open: boolean) => void
    - onSubmit: (data: CreateTemplateInput) => Promise<void>
    
    Form fields:
    - Template name (text input, required)
    - Category (select: wedding, birthday, custom, required)
    - Thumbnail URL (text input, required)
    - Preview button (shows template preview)
    - Submit button (creates template)
    
    Validation:
    - Name: 1-255 characters, required
    - Category: required
    - Thumbnail: valid URL format
    
    UI:
    - Use shadcn/ui Dialog, Form, Input, Select, Button
    - Preview section shows template preview
    - Loading state during submission
    - Error display for validation/server errors
    - Success message on creation
    
    Reference Phase 2 forms (LoginForm, SignupForm) for form patterns.
    Use shadcn/ui Form components.
  </action>
  <verify>
    <automated>cd /media/jay/D/cursor/todayis && npx tsc --noEmit src/components/templates/library/TemplateUploadDialog.tsx</automated>
  </verify>
  <done>
    TemplateUploadDialog component created
    Form with name, category, thumbnail fields
    Preview section shows template preview
    Validation and error handling
    TypeScript compilation passes
  </done>
</task>

<task type="auto">
  <name>Task 4: Create Templates page</name>
  <files>src/app/(main)/templates/page.tsx</files>
  <action>
    Create templates page that wires everything together:
    
    Features:
    - Fetch templates from /api/templates on mount
    - Handle loading state (spinner/skeleton)
    - Handle auth redirect (if not logged in)
    - Pass templates to TemplateLibrary
    - Handle create, edit, delete actions
    - Show success/error toasts
    
    State:
    - templates: Template[]
    - loading: boolean
    - error: string | null
    - selectedTemplate: Template | null (for edit)
    
    Actions:
    - onCreate: Open upload dialog
    - onSelect: Open editor for selected template
    - onDelete: Delete template with confirmation
    - onFilter: Filter templates by category
    - onSearch: Filter templates by name
    
    Layout:
    - Header with title and "Create New" button
    - Filter and search controls
    - TemplateLibrary component
    - Toast notifications for actions
    
    Reference Phase 2 dashboard for auth handling.
    Use shadcn/ui Toast, AlertDialog components.
  </action>
  <verify>
    <automated>cd /media/jay/D/cursor/todayis && npx tsc --noEmit src/app/(main)/templates/page.tsx</automated>
  </verify>
  <done>
    Templates page created
    Fetches templates from API
    Handles loading and auth states
    Wires all actions (create, edit, delete, filter, search)
    TypeScript compilation passes
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 5: Verify template library UI</name>
  <files>src/app/(main)/templates/page.tsx</files>
  <action>
    What was built:
    - Complete template library UI with grid layout
    - TemplateCard components for individual templates
    - TemplateUploadDialog for creating new templates
    - Filter, search, and CRUD operations
    
    How to verify:
    1. Navigate to http://localhost:3000/templates
    2. Verify templates display in grid layout
    3. Test category filter (should filter templates)
    4. Test search (should filter by name)
    5. Click "Create New" button (should open dialog)
    6. Create a new template (should appear in grid)
    7. Click edit on a template (should open editor)
    8. Delete a template (should show confirmation, then remove)
    
    Expected behavior:
    - Grid layout responsive (1 col mobile, 3-4 col desktop)
    - All CRUD operations work
    - Loading states show during operations
    - Error messages display for failures
  </action>
  <verify>
    <automated>Visit http://localhost:3000/templates and verify all UI elements render correctly</automated>
  </verify>
  <done>
    User confirms:
    - Templates display in grid
    - Filter and search work
    - Create, edit, delete operations functional
    - UI is responsive and consistent
  </done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. UI rendering:
   - Templates page loads without errors
   - Grid layout displays correctly
   - All components render with proper styling

2. Functionality:
   - Filter by category works
   - Search by name works
   - Create new template works
   - Edit template works
   - Delete template works

3. State management:
   - Loading states display correctly
   - Empty states show when no templates
   - Error states display properly

4. Responsive design:
   - Mobile: 1 column grid
   - Tablet: 2 column grid
   - Desktop: 3-4 column grid
</verification>

<success_criteria>
- src/app/(main)/templates/page.tsx created
- src/components/templates/library/TemplateLibrary.tsx created
- src/components/templates/library/TemplateCard.tsx created
- src/components/templates/library/TemplateUploadDialog.tsx created
- All CRUD operations functional
- Filter and search working
- UI responsive and consistent
- User verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/03-template-management/03-03-SUMMARY.md`
</output>
