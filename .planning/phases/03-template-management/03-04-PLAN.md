---
phase: 03-template-management
plan: 04
type: execute
wave: 4
depends_on: [03-03]
gap_closure: true
files_modified: [src/app/(main)/templates/page.tsx, src/components/templates/library/TemplateUploadDialog.tsx]
autonomous: false
requirements: [TEMPLATE-MANAGE-03]
---

<objective>
Fix incomplete edit and upload functionality

Purpose: Complete the 2 gaps identified in verification:
1. Implement navigation to template editor when editing
2. Add fields/layout configuration to upload dialog
</objective>

<execution_context>
@/home/jay/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/jay/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-template-management/03-template-management-VERIFICATION.md
@src/app/(main)/templates/page.tsx
@src/components/templates/library/TemplateUploadDialog.tsx
@src/types/template.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement edit navigation</name>
  <files>src/app/(main)/templates/page.tsx</files>
  <action>
    Replace stubbed handleSelect with actual navigation to editor:
    
    1. Import useRouter from next/navigation
    2. Replace toast notification with: router.push(`/templates/${template.id}/edit`)
    3. Ensure template ID is passed in URL
  </action>
  <verify>
    <automated>cd /media/jay/D/cursor/todayis && grep -n "router.push.*edit" src/app/\(main\)/templates/page.tsx</automated>
  </verify>
  <done>
    handleSelect navigates to /templates/[id]/edit
    Toast notification removed
    TypeScript compilation passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Add fields configuration to upload dialog</name>
  <files>src/components/templates/library/TemplateUploadDialog.tsx</files>
  <action>
    Add fields configuration form to TemplateUploadDialog:
    
    1. Add dynamic field builder:
       - "Add Field" button
       - Field items with: name, type (text/date/image/location), label, required
       - Remove field button
       - Default field preview
    
    2. State management:
       - fields: TemplateField[]
       - addField(): adds new field
       - removeField(index): removes field at index
       - updateField(index, data): updates field
    
    3. Include fields in form submission
  </action>
  <verify>
    <automated>cd /media/jay/D/cursor/todayis && grep -n "addField\|removeField" src/components/templates/library/TemplateUploadDialog.tsx</automated>
  </verify>
  <done>
    Fields configuration UI added
    addField/removeField/updateField functions implemented
    Fields included in form submission
  </done>
</task>

<task type="auto">
  <name>Task 3: Add layout configuration to upload dialog</name>
  <files>src/components/templates/library/TemplateUploadDialog.tsx</files>
  <action>
    Add layout configuration to TemplateUploadDialog:
    
    1. Add layout selector (predefined layouts or custom):
       - Simple: single column
       - Classic: header + content + footer
       - Modern: grid-based
    
    2. Store layout as JSON configuration
    3. Include layout in form submission
  </action>
  <verify>
    <automated>cd /media/jay/D/cursor/todayis && grep -n "layout" src/components/templates/library/TemplateUploadDialog.tsx | head -5</automated>
  </verify>
  <done>
    Layout configuration UI added
    Layout saved as JSON
    Layout included in form submission
  </done>
</task>

<task type="auto">
  <name>Task 4: Update handleCreate to send complete data</name>
  <files>src/app/(main)/templates/page.tsx</files>
  <action>
    Update handleCreate to include fields and layout:
    
    1. Destructure fields and layout from form data
    2. Include in POST request body:
       { name, category, thumbnail, fields, layout }
    3. Handle API response success
  </action>
  <verify>
    <automated>cd /media/jay/D/cursor/todayis && grep -A5 "fetch.*\/api\/templates" src/app/\(main\)/templates/page.tsx | head -10</automated>
  </verify>
  <done>
    handleCreate includes fields and layout in POST body
    API response handled correctly
  </done>
</task>

</tasks>

<verification>
1. Edit navigation:
   - Click Edit button navigates to /templates/[id]/edit
   - URL contains template ID

2. Upload dialog:
   - Can add/remove template fields
   - Can select layout configuration
   - Form validates all required fields

3. API integration:
   - POST /api/templates receives complete data
   - Template created with fields and layout
</verification>

<success_criteria>
- handleSelect navigates to editor page
- TemplateUploadDialog includes fields configuration
- TemplateUploadDialog includes layout configuration
- POST request includes fields and layout
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-template-management/03-04-SUMMARY.md`
</output>