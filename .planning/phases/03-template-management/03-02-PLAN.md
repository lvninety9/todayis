---
phase: 03-template-management
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [src/app/api/templates/route.ts, src/app/api/templates/[id]/route.ts]
autonomous: true
requirements: [TEMPLATE-MANAGE-01]
user_setup:
  - service: supabase
    why: "API routes need Supabase client for database operations"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> Settings -> API"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Settings -> API"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Settings -> API"

must_haves:
  truths:
    - GET /api/templates returns all user's templates
    - POST /api/templates creates a new template
    - GET /api/templates/[id] returns specific template
    - PATCH /api/templates/[id] updates template
    - DELETE /api/templates/[id] deletes template
    - All endpoints enforce user ownership
  artifacts:
    - path: src/app/api/templates/route.ts
      provides: "Template CRUD endpoints (list, create)"
      exports: ["GET", "POST"]
    - path: src/app/api/templates/[id]/route.ts
      provides: "Template CRUD endpoints (get, update, delete)"
      exports: ["GET", "PATCH", "DELETE"]
  key_links:
    - from: src/app/api/templates/route.ts
      to: src/lib/supabase/database.types.ts
      via: "type imports"
      pattern: "import.*Template"
    - from: src/app/api/templates/route.ts
      to: src/hooks/use-session.ts
      via: "user authentication"
      pattern: "useSession|supabase.auth"

---

<objective>
Implement template CRUD API endpoints

Purpose: Provide RESTful API for template management operations

Output: Template API routes with authentication and ownership validation
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-template-management/03-01-SUMMARY.md

# Existing Code Context
@src/app/api/auth/logout/route.ts
@src/hooks/use-session.ts
@src/lib/template-utils.ts
@src/types/template.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From src/app/api/auth/logout/route.ts:
```typescript
import { NextResponse } from 'next/server';
import { createClient } from '@supabase/supabase-js';

const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL!;
const supabaseServiceKey = process.env.SUPABASE_SERVICE_ROLE_KEY!;

export async function POST() {
  const supabase = createClient(supabaseUrl, supabaseServiceKey);
  // ... auth operations
}
```

From src/hooks/use-session.ts:
```typescript
export interface Session {
  user: User | null;
  session: AuthSession | null;
  loading: boolean;
  error: string | null;
}
```

From src/types/template.ts:
```typescript
export interface Template {
  id: string;
  userId: string;
  name: string;
  category: string;
  thumbnail: string;
  fields: TemplateField[];
  layout: string;
  isPublished: boolean;
  downloadCount: number;
  createdAt: string;
  updatedAt: string;
}
```
</interfaces>

</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GET /api/templates endpoint</name>
  <files>src/app/api/templates/route.ts</files>
  <action>
    Implement GET endpoint to fetch user's templates:
    
    1. Setup Supabase client with service role key
    2. Extract user from request headers (use Supabase auth helper)
    3. Query templates where user_id matches current user
    4. Filter by is_published if query param ?published=true
    5. Sort by created_at descending
    6. Return JSON: { templates: Template[], count: number }
    
    Error handling:
    - 401 if unauthorized
    - 400 if query params invalid
    - 500 for database errors
    
    Follow patterns from Phase 2 auth API (src/app/api/auth/logout/route.ts).
    Use consistent error response format: { error: string }.
  </action>
  <verify>
    <automated>cd /media/jay/D/cursor/todayis && curl -X GET http://localhost:3000/api/templates -H "Authorization: Bearer test-token" 2>&1 | grep -E "(templates|error|401)"</automated>
  </verify>
  <done>
    GET /api/templates returns user's templates
    Supports ?published=true filter
    Returns 401 for unauthorized requests
    Response format: { templates: [...], count: number }
  </done>
</task>

<task type="auto">
  <name>Task 2: Create POST /api/templates endpoint</name>
  <files>src/app/api/templates/route.ts</files>
  <action>
    Implement POST endpoint to create new template:
    
    1. Parse request body: { name, category, thumbnail, fields, layout }
    2. Validate required fields (name, category, fields)
    3. Extract user from request (same as GET)
    4. Insert into templates table with user_id
    5. Generate unique id (uuid)
    6. Set is_published: false, downloadCount: 0 by default
    7. Return created template with 201 status
    
    Validation:
    - name: 1-255 characters
    - category: non-empty string
    - fields: array of TemplateField
    - Return 400 if validation fails
    
    Error handling:
    - 401 if unauthorized
    - 400 if validation fails
    - 500 for database errors
    
    Reference Phase 2 validation patterns.
    Use consistent error responses.
  </action>
  <verify>
    <automated>cd /media/jay/D/cursor/todayis && curl -X POST http://localhost:3000/api/templates -H "Content-Type: application/json" -d '{"name":"Test","category":"wedding","thumbnail":"url","fields":[]}' 2>&1 | grep -E "(201|error|created)"</automated>
  </verify>
  <done>
    POST /api/templates creates new template
    Validates required fields
    Returns 201 with created template
    Returns 400 for invalid input
  </done>
</task>

<task type="auto">
  <name>Task 3: Create GET/PATCH/DELETE /api/templates/[id] endpoints</name>
  <files>src/app/api/templates/[id]/route.ts</files>
  <action>
    Implement single template operations:
    
    GET [id]:
    - Fetch template by id
    - Verify user ownership (userId matches current user)
    - Return template or 404 if not found
    - Increment downloadCount if ?increment=true
    
    PATCH [id]:
    - Parse partial update object
    - Verify user ownership before update
    - Update only provided fields
    - Update updatedAt timestamp
    - Return updated template
    
    DELETE [id]:
    - Verify user ownership before delete
    - Delete template from database
    - Return { message: "Deleted successfully" }
    
    All endpoints:
    - 401 if unauthorized
    - 404 if template not found
    - 403 if user doesn't own template
    - 500 for database errors
    
    Follow ownership validation pattern from Phase 2.
  </action>
  <verify>
    <automated>cd /media/jay/D/cursor/todayis && curl -X GET http://localhost:3000/api/templates/test-id 2>&1 | grep -E "(404|template|error)"</automated>
  </verify>
  <done>
    GET /api/templates/[id] returns template with ownership check
    PATCH /api/templates/[id] updates template with ownership check
    DELETE /api/templates/[id] deletes template with ownership check
    All endpoints return 403 for unauthorized ownership
  </done>
</task>

</tasks>

<verification>
1. API endpoint tests:
   - GET /api/templates returns list
   - POST /api/templates creates template
   - GET /api/templates/[id] returns single template
   - PATCH /api/templates/[id] updates template
   - DELETE /api/templates/[id] deletes template

2. Authentication:
   - All endpoints require auth
   - 401 returned for missing/invalid auth

3. Ownership validation:
   - Users can only modify their own templates
   - 403 returned for unauthorized access

4. Error handling:
   - Consistent error response format
   - Proper HTTP status codes
</verification>

<success_criteria>
- src/app/api/templates/route.ts created with GET and POST
- src/app/api/templates/[id]/route.ts created with GET, PATCH, DELETE
- All endpoints enforce authentication
- All endpoints validate user ownership
- Proper error handling and status codes
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-template-management/03-02-SUMMARY.md`
</output>
