---
phase: 15-field-extension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/template.ts
  - src/components/templates/editor/FieldEditor. tsx
autonomous: true
requirements:
  - FIELD-01
  - FIELD-02
  - FIELD-03
  - FIELD-04
  - FIELD-05
  - FIELD-06
  - FIELD-07
  - FIELD-08
user_setup: []
---

<objective>
Extend template field types from 4 to 11 (add account, audio, video, gallery, message, dresscode, parents). Per D-01 to D-24 from CONTEXT. md.
Purpose: Enable wedding-specific fields for account info, music, video, gallery, messages, dress codes, and parents.
Output: Extended FieldType union + FieldEditor with 7 new switch cases.
</objective>

<context>
@src/ types/ template. ts
@src/ components/ templates/ editor/ FieldEditor. tsx
@. planning/ phases/15- field- extension/15- CONTEXT. md
</context>

<interfaces>
From src/types/ template. ts (lines 11-29):
```typescript
export type FieldType = 'text' | 'date' | 'image' | 'location';  // LINE 13
export interface TemplateField {
  name: string;
  type: FieldType;
  label: string;
  required: boolean;
  defaultValue: string | null;
}
```

From src/ components/ templates/ editor/ FieldEditor. tsx (lines 44-138):
```typescript
// Existing switch pattern:
switch (field. type) {
  case 'text': return <input type="text" ... />
  case 'date': return <div>...</div>
  case 'image': return <div>...</div>
  case 'location': return <div>...</div>
  default: return <input ... />
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Extend FieldType union with 7 new types</name>
  <files>src/ types/ template. ts</files>
  <action>
    Extend FieldType union (line 13) from:
    'text' | 'date' | 'image' | 'location'
    To:
    'text' | 'date' | 'image' | 'location' | 'account' | 'audio' | 'video' | 'gallery' | 'message' | 'dresscode' | 'parents'
    
    Per D-09 decision: account, audio, video, gallery, message, dresscode, parents added.
    Per D-06: GIF already supported in existing image type.
  </action>
  <verify>
    <automated>grep -n "FieldType = " src/ types/ template. ts</automated>
  </verify>
  <done>FieldType union has 11 types</done>
</task>

<task type="auto">
  <name>Add 7 new switch cases to FieldEditor</name>
  <files>src/ components/ templates/ editor/ FieldEditor. tsx</files>
  <action>
    Add 7 new cases to switch statement after 'location' case (line 124):
    
    1. **case 'account'** (per D-14): Bank select + account number input + account holder input
       - Bank list: KB, NH, WR, SH, IBK, etc.
       - Use shadcn Select component for bank selection
       
    2. **case 'audio'** (per D-11): URL input + Audio player preview
       - Accept MP3, OGG, WAV URLs
       - Preview with basic audio element
       
    3. **case 'video'** (per D-12): URL input + Video player preview
       - Accept MP4 URLs
       - Preview with video element
       
    4. **case 'gallery'** (per D-13): Multi-image URL input
       - Comma-separated URLs or array input
       - Basic image previews
       
    5. **case 'message'** (per D-16): Textarea for long text
       - Multiple lines input
       - Label: "축의도 메시지"
       
    6. **case 'dresscode'** (per D-17): Text input + DressCodeBadge display
       - e.g., "Smart Casual", "한복"
       
    7. **case 'parents'** (per D-15): 4 field group
       - 신랑父, 신랑母, 신부父, 신부母
    
    Use warm palette classes (terracotta, sage, blush) per Phase 13 design system.
    Reference Phase 14 UX-03 for color tokens: --color-terracotta, --color-sage, --color-blush.
  </action>
  <verify>
    <automated>grep -c "case 'account'\|case 'audio'\|case 'video'\|case 'gallery'\|case 'message'\|case 'dresscode'\|case 'parents'" src/ components/ templates/ editor/ FieldEditor. tsx</automated>
  </verify>
  <done>7 new cases added, all compile</done>
</task>

</tasks>

<verification>
- `npm run type-check` passes
- All 11 FieldType cases handled in FieldEditor switch
</verification>

<success_criteria>
- FieldType union has 11 types
- FieldEditor handles all 11 types
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `. planning/ phases/15- field- extension/15-01-SUMMARY. md`
</output>