---
wave: 2
depends_on:
  - 16-01-PLAN.md
files_modified:
  - src/app/(main)/landing/page.tsx
  - src/app/(main)/dashboard/page.tsx
  - src/app/(main)/templates/page.tsx
  - src/app/(main)/templates/[id]/page.tsx
autonomous: false
requirements_addressed:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
---

# Wave 2: Core Pages (UI-01, UI-02, UI-03, UI-04)

**Goal**: Landing, Dashboard, Template Library, Template Detail 페이지 리디자인

**must_haves**:
- Landing: hero 섹션 modern design, social proof 추가
- Dashboard: bento grid 개선, 데이터 시각화
- Template Library: 필터/정렬 개선, 그리드/리스트 토글
- Template Detail: 구매 플로우 최적화, 미리보기 확대

## Task 1: Landing Page Modernization (UI-01)

<task>
<read_first>
- src/app/(main)/landing/page.tsx
</read_first>

<acceptance_criteria>
- Hero 섹션: gradient text 적용됨
- Social proof 섹션 추가됨 (별점, 리뷰 수, 사용자 수)
- 스토리텔링 섹션 추가 (3단계 프로세스)
- CTA가 페이지 내부에 2곳 이상 존재
</acceptance_criteria>

<action>
landing/page.tsx를 다음과 같이 업데이트:

1. Hero 섹션:
```tsx
<h1 className="text-5xl lg:text-7xl font-bold bg-gradient-to-r from-[--primary] via-[--accent] to-[--primary] bg-clip-text text-transparent">
  소중한 순간, 특별한 초대장
</h1>
```

2. Social proof 추가:
```tsx
<div className="flex items-center gap-4 text-sm text-gray-500">
  <span>⭐ 4.9 (120+ 리뷰)</span>
  <span>•</span>
  <span>1,200+명이 선택</span>
</div>
```

3. 스토리텔링 섹션 추가:
```tsx
<section className="py-16">
  <h2>3단계로 완성하는 나만의 초대장</h2>
  <div className="grid grid-cols-3 gap-8">
    <div>1. 템플릿 선택</div>
    <div>2. 내용 입력</div>
    <div>3. 공유하기</div>
  </div>
</section>
```

4. CTA複数追加: Hero, Features, CTA section minimum 3개
</action>
</task>

## Task 2: Dashboard Enhancement (UI-02)

<task>
<read_first>
- src/app/(main)/dashboard/page.tsx
</read_first>

<acceptance_criteria>
- Bento grid 개선됨 (col-span, row-span 적절히 사용)
- 데이터 시각화 컴포넌트 추가 (통계 차트)
- 액션 중심 레이아웃 (高频アクション 상단 배치)
- Recent templates에 thumbnail + quick actions
</acceptance_criteria>

<action>
dashboard/page.tsx를 다음과 같이 업데이트:

1. Bento grid 개선:
```tsx
<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
  <GlassCard className="lg:col-span-2 lg:row-span-2"> {/* Template stats */}
  <GlassCard className="lg:col-span-1"> {/* Invitation count */}
  <GlassCard className="lg:col-span-1"> {/* Purchase count */}
```

2. 데이터 시각화 (선택적 - recharts 또는 simple CSS):
```tsx
<div className="text-xs text-gray-400">
  전월 대비 +15% 증가
</div>
```

3. Recent templates quick actions:
```tsx
<div className="flex gap-2">
  <Button size="sm" variant="ghost">편집</Button>
  <Button size="sm" variant="ghost">공유</Button>
  <Button size="sm" variant="ghost">발행</Button>
</div>
```
</action>
</task>

## Task 3: Template Library UX (UI-03)

<task>
<read_first>
- src/app/(main)/templates/page.tsx
</read_first>

<acceptance_criteria>
- 필터/정렬 UI 개선됨
- 그리드/리스트 토글 버튼 추가됨
- Hover 시 미리보기 (modal 또는 slide-in) 추가됨
- 반응형グリッド維持
</acceptance_criteria>

<action>
templates/page.tsx를 다음과 같이 업데이트:

1. 그리드/리스트 토글:
```tsx
const [viewMode, setViewMode] = useState<'grid' | 'list'>('grid');

<Button variant="ghost" size="sm" onClick={() => setViewMode(v => v === 'grid' ? 'list' : 'grid')}>
  {viewMode === 'grid' ? <ListIcon /> : <GridIcon />}
</Button>
```

2. 필터/정렬:
```tsx
<Select>
  <SelectTrigger>정렬</SelectTrigger>
  <SelectContent>
    <SelectItem value="latest">최신순</SelectItem>
    <SelectItem value="popular">인기순</SelectItem>
    <SelectItem value="name">이름순</SelectItem>
  </SelectContent>
</Select>
```

3. Hover 미리보기 (선택적):
```tsx
<Card onMouseEnter={() => setPreview(template)}>
  {preview && <PreviewModal template={preview} />}
</Card>
```
</action>
</task>

## Task 4: Template Detail Optimization (UI-04)

<task>
<read_first>
- src/app/(main)/templates/[id]/page.tsx
</read_first>

<acceptance_criteria>
- 템플릿 미리보기 확대됨
- 구매 버튼 UX 개선 (CTA顯著)
- 모바일에서 미리보기 확대 기능
</acceptance_criteria>

<action>
templates/[id]/page.tsx를 다음과 같이 업데이트:

1. 미리보기 확대:
```tsx
<Dialog>
  <DialogTrigger>
    <Button variant="outline">미리보기 확대</Button>
  </DialogTrigger>
  <DialogContent className="max-w-4xl">
    <InvitationViewer template={template} />
  </DialogContent>
</Dialog>
```

2. 구매 버튼 CTA:
```tsx
<div className="sticky bottom-4 z-10">
  <Button size="lg" className="w-full">구매하기</Button>
</div>
```

3. 모바일 미리보기:
```tsx
<Tabs defaultValue="preview">
  <TabsList>
    <TabsTrigger value="preview">미리보기</TabsTrigger>
    <TabsTrigger value="details">상세정보</TabsTrigger>
  </TabsList>
</Tabs>
```
</action>
</task>