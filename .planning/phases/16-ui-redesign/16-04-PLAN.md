---
wave: 4
depends_on:
  - 16-03-PLAN.md
files_modified:
  - src/app/(main)/[username]/page.tsx
  - src/app/(main)/layout.tsx
autonomous: false
requirements_addressed:
  - UI-07
  - UI-08
---

# Wave 4: Public Invitation & Navigation (UI-07, UI-08)

**Goal**: 공개 초대장 웨딩 테마 강화, 네비게이션 재설계

**must_haves**:
- Public Invitation: 웨딩 ROMANTIC 디자인 강화, 애니메이션 개선
- Navigation: 글로벌 네비 개선, 모바일 바 최적화, breadcrumbs 추가

## Task 1: Public Invitation Enhancement (UI-07)

<task>
<read_first>
- src/app/(main)/[username]/page.tsx
- src/components/publish/InvitationViewer.tsx
</read_first>

<acceptance_criteria>
- Wedding ROMANTIC 테마 강화 (weddings 전용 애니메이션)
- 공유하기 버튼이顯著하게 배치됨
- 모바일에서 미리보기 개선
</acceptance_criteria>

<action>
[username]/page.tsx를 다음과 같이 업데이트:

1. Wedding ROMANTIC 애니메이션:
```tsx
<motion.div
  initial={{ opacity: 0, y: 20 }}
  animate={{ opacity: 1, y: 0 }}
  transition={{ duration: 0.8, ease: "easeOut" }}
>
  {/* Wedding content */}
</motion.div>
```

2. 공유하기 버튼:
```tsx
<div className="fixed bottom-4 right-4 z-50">
  <ShareButton invitation={invitation} />
</div>
```

3. 모바일 미리보기:
```tsx
<div className="touch-manipulation">
  <InvitationViewer template={invitation.template} />
</div>
```
</action>
</task>

## Task 2: Share UX Improvement

<task>
<read_first>
- src/components/publish/ShareButton.tsx
- src/components/publish/ShareDialog.tsx
</read_first>

<acceptance_criteria>
- 공유 옵션 최소 3개 (링크 복사, Kakao, SNS)
- 링크 복사 시 피드백 제공
- QR 코드 추가
</acceptance_criteria>

<action>
ShareButton/ShareDialog을 다음과 같이 개선:

```tsx
const shareOptions = [
  { icon: LinkIcon, label: '링크 복사', action: copyLink },
  { icon: KakaoIcon, label: '카카오톡', action: shareKakao },
  { icon: SNSIcon, label: 'SNS', action: shareSNS },
];

<Dialog>
  <DialogTrigger>
    <Button><ShareIcon /> 공유하기</Button>
  </DialogTrigger>
  <DialogContent>
    {/* Link copy */}
    <Input value={url} readOnly />
    <Button onClick={copyLink}>복사</Button>
    
    {/* QR Code */}
    <QRCode value={url} />
  </DialogContent>
</Dialog>
```
</action>
</task>

## Task 3: Navigation Redesign (UI-08)

<task>
<read_first>
- src/app/(main)/layout.tsx
</read_first>

<acceptance_criteria>
- 글로벌 네비 개선 (glassmorphism, sticky)
- 모바일 바 최적화 (icon + label,-safe-area対応)
- Breadcrumbs 추가됨
</acceptance_criteria>

<action>
layout.tsx 네비게이션을 다음과 같이 업데이트:

1. Glassmorphism Nav:
```tsx
<nav className="sticky top-0 z-50 backdrop-blur-md bg-white/80 dark:bg-black/80 border-b border-gray-200 dark:border-gray-800">
  <div className="max-w-7xl mx-auto px-4">
    <Logo />
    <NavLinks className="hidden md:flex" />
    <MobileNav className="md:hidden" />
  </div>
</nav>
```

2. Mobile Bottom Bar:
```tsx
<nav className="fixed bottom-0 left-0 right-0 bg-white dark:bg-black border-t border-gray-200 dark:border-gray-800 pb-safe">
  <div className="flex justify-around">
    <MobileNavItem icon={Home} label="홈" href="/" />
    <MobileNavItem icon={Template} label="템플릿" href="/templates" />
    <MobileNavItem icon={Plus} label="만들기" href="/create" />
    <MobileNavItem icon={User} label="내정보" href="/settings" />
  </div>
</nav>
```

3. Breadcrumbs:
```tsx
<Breadcrumbs>
  <BreadcrumbItem href="/">홈</BreadcrumbItem>
  <BreadcrumbItem href="/templates">템플릿</BreadcrumbItem>
  <BreadcrumbItem>현재</BreadcrumbItem>
</Breadcrumbs>
```
</action>
</task>