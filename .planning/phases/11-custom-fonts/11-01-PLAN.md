---
phase: 11-custom-fonts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/publish/InvitationViewer.tsx
  - src/lib/fonts.ts
  - src/app/layout.tsx
autonomous: true
requirements: [FONT-01, FONT-02]
must_haves:
  truths:
    - "사용자가 템플릿 편집기에서 선택한 폰트가 초대장에 적용된다"
    - "초대장 방문자가 커스텀 폰트를 확인할 수 있다"
  artifacts:
    - path: "src/lib/fonts.ts"
      provides: "폰트 로딩 유틸리티"
      min_lines: 30
    - path: "src/components/publish/InvitationViewer.tsx"
      provides: "폰트 적용된 초대장 렌더링"
      exports: ["InvitationViewer"]
  key_links:
    - from: "StyleEditor"
      to: "InvitationViewer"
      via: "layout.fontFamily"
      pattern: "fontFamily.*layout"
---

<objective>
사용자가 템플릿 편집기(StyleEditor)에서 선택한 폰트가 공개 초대장(InvitationViewer)에 제대로 적용되도록 구현한다.

Purpose: Phase 09에서 StyleEditor에 폰트 선택 옵션은 추가되었으나, InvitationViewer에 실제로 로드되지 않아 웹페이지에 적용되지 않았음. 이Plan으로 완결성 확보.

Output: InvitationViewer에서 fontFamily로 선택한 폰트가 정상 렌더링됨
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/templates/editor/StyleEditor.tsx
@src/types/template.ts
@src/lib/supabase/database.types.ts

From StyleEditor (lines 74-81):
```typescript
const fontFamilyOptions = [
  { value: 'inherit', label: '기본값' },
  { value: 'Pretendard', label: 'Pretendard' },
  { value: 'Noto Sans KR', label: 'Noto Sans KR' },
  { value: 'Nanum Gothic', label: 'Nanum Gothic' },
  { value: 'Open Sans', label: 'Open Sans' },
  { value: 'Playfair Display', label: 'Playfair Display' },
];
```

From layout in InvitationViewer (line 26):
```typescript
const layout = invitation.layout as Record<string, unknown> || {};
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create font utility library</name>
  <files>src/lib/fonts.ts</files>
  <behavior>
    - Test 1: getFontFamily returns correct font family string based on fontFamily option
    - Test 2: getFontCSSVar returns CSS variable name for font
    - Test 3: loadGoogleFont creates link tag for Google Font
  </behavior>
  <action>Create src/lib/fonts.ts with:
- FontOption type: 'inherit' | 'Pretendard' | 'Noto Sans KR' | 'Nanum Gothic' | 'Open Sans' | 'Playfair Display' | 'custom'
- getFontFamily(fontFamily: string): string - returns CSS font-family value
- getFontCSSVar(fontFamily: string): string - returns CSS variable name (--font-[name])
- loadGoogleFont(fontFamily: string): string - returns Google Fonts URL for preconnect
- Predefine Google Fonts URLs for: Pretendard, Noto Sans KR, Nanum Gothic, Open Sans, Playfair Display

Use Google Fonts API format: https://fonts.googleapis.com/css2?family=Pretendard:wght@400;500;600;700&display=swap</action>
  <verify>
    <automated>npm test -- --filter=fonts 2>/dev/null || echo "No tests yet - manual verification"</automated>
  </verify>
  <done>fonts.ts exists with getFontFamily, getFontCSSVar, loadGoogleFont exports</done>
</task>

<task type="auto">
  <name>Task 2: Integrate fonts into layout.tsx</name>
  <files>src/app/layout.tsx</files>
  <action>Update src/app/layout.tsx to:
- Import loadGoogleFont from @/lib/fonts
- Add preconnect for Google Fonts in metadata
- Add font-display: swap for performance

Already has Noto_Serif_KR - ADD other fonts needed for invitations:
```typescript
// Add after Noto_Serif_KR import
const pretendard = Pretendard({ 
  subsets: ['latin'],
  variable: '--font-pretendard',
  display: 'swap',
});
```</action>
  <verify>
    <automated>npm run build 2>&1 | head -20</automated>
  </verify>
  <done>layout.tsx includes font CSS variables for invitation fonts</done>
</task>

<task type="auto">
  <name>Task 3: Apply fonts in InvitationViewer</name>
  <files>src/components/publish/InvitationViewer.tsx</files>
  <action>Update InvitationViewer component to:
- Import getFontFamily from @/lib/fonts
- Extract fontFamily from layout: const fontFamily = (layout as Record<string, string>).fontFamily || 'inherit'
- Apply font to root container: style={{ fontFamily: getFontFamily(fontFamily) }}
- For custom fonts, use CSS variable: style={{ fontFamily: fontFamily === 'custom' ? 'var(--font-custom)' : getFontFamily(fontFamily) }}

Add to InvitationViewer container: 
```typescript
<div 
  className="min-h-screen..."
  style={{ 
    fontFamily: getFontFamily(layout.fontFamily || 'inherit'),
    ...otherStyles 
  }}
>
```</action>
  <verify>
    <automated>npm run type-check 2>&1 | grep -E "(Error|error)" | head -10</automated>
  </verify>
  <done>InvitationViewer applies fontFamily from layout</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Custom Fonts Integration (fonts.ts + layout.tsx + InvitationViewer)</what-built>
  <how-to-verify>
    1. Create or edit a template in /templates/[id]/edit
    2. Go to Style tab, select different font (e.g., "Playfair Display")
    3. Save template, create invitation
    4. Visit public URL like /[username]
    5. Inspect element, verify font-family is applied
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- fonts.ts has getFontFamily, getFontCSSVar functions
- layout.tsx includes font CSS variables
- InvitationViewer applies fontFamily from layout
- human verification confirms font renders correctly
</verification>

<success_criteria>
- StyleEditor에서 "Playfair Display" 선택 시 초대장에 해당 폰트가 적용됨
- 브라우저 개발자 도구로 font-family 확인 가능
</success_criteria>

<output>
After completion, create `.planning/phases/11-custom-fonts/{phase}-01-SUMMARY.md`
</output>