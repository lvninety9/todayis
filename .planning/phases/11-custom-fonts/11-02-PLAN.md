---
phase: 11-custom-fonts
plan: 02
type: execute
wave: 1
depends_on: [11-01]
files_modified:
  - src/app/api/fonts/route.ts
  - src/components/templates/editor/StyleEditor.tsx
  - src/components/publish/InvitationViewer.tsx
  - src/lib/supabase/database.types.ts
autonomous: true
requirements: [FONT-03]
must_haves:
  truths:
    - "사용자가 자신의 커스텀 폰트를 업로드할 수 있다"
    - "업로드한 커스텀 폰트가 초대장에 적용된다"
  artifacts:
    - path: "src/app/api/fonts/route.ts"
      provides: "커스텀 폰트 업로드 API"
      exports: ["POST", "DELETE"]
    - path: "src/lib/fonts.ts"
      provides: "커스텀 폰트 로딩"
      exports: ["getCustomFont"]
  key_links:
    - from: "StyleEditor font upload"
      to: "/api/fonts"
      via: "FormData upload"
      pattern: "POST.*api/fonts"
---

<objective>
사용자가 자신의 커스텀 폰트(.ttf, .otf, .woff, .woff2)를 업로드하고 템플릿에 적용할 수 있도록 구현한다.

Purpose: Phase 09에서 기본 제공 폰트 외에 사용자가 직접 폰트를 업로드하여 고유한 초대장을 만들 수 있게 함.

Output: 커스텀 폰트 업로드 API + StyleEditor 연동
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/templates/editor/StyleEditor.tsx
@src/app/api/templates/media/route.ts
@src/lib/supabase/database.types.ts

From Phase 09 media upload as reference:
```typescript
// src/app/api/templates/media/route.ts
// Supports .mp3, .wav, .ogg, max 10MB
// Storage bucket: template-media
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /api/fonts endpoint</name>
  <files>src/app/api/fonts/route.ts</files>
  <action>Create API route for custom font upload:

```typescript
// POST /api/fonts - Upload custom font
// DELETE /api/fonts - Delete custom font
```

Features:
- Accept: .ttf, .otf, .woff, .woff2 (max 5MB)
- Use Supabase Storage bucket: `custom-fonts`
- Store font file URL in user metadata or separate table
- Return: { success, fontUrl, fontFamily }

Also create:
- supabase/storage/upload-font.js (helper script for bucket creation)
- or auto-create bucket in route handler</action>
  <verify>
    <automated>curl -X POST http://localhost:3000/api/fonts 2>&1 | head -5</automated>
  </verify>
  <done>/api/fonts route exists with POST/DELETE handlers</done>
</task>

<task type="auto">
  <name>Task 2: Update fonts.ts for custom fonts</name>
  <files>src/lib/fonts.ts</files>
  <action>Update src/lib/fonts.ts to support custom fonts:

```typescript
// Add after existing exports
export interface CustomFont {
  id: string;
  name: string;
  url: string;
  family: string;
}

export function getCustomFont(fontUrl: string): string {
  // Returns CSS @font-face rule for custom font
  return `
    @font-face {
      font-family: 'CustomFont';
      src: url('${fontUrl}') format('truetype');
      font-display: swap;
    }
  `;
}

export function loadCustomFontCSS(fontUrls: string[]): string {
  // Returns full CSS with multiple @font-face rules
  return fontUrls.map(url => getCustomFont(url)).join('\n');
}
```</action>
  <verify>
    <automated>npm run type-check 2>&1 | grep -E "(Error|error)" | head -10</automated>
  </verify>
  <done>fonts.ts exports getCustomFont, loadCustomFontCSS</done>
</task>

<task type="auto">
  <name>Task 3: Add font upload to StyleEditor</name>
  <files>src/components/templates/editor/StyleEditor.tsx</files>
  <action>Update StyleEditor to add font upload option:

Add to fontFamilyOptions:
```typescript
{ value: 'custom', label: '커스텀 폰트' },
```

Add upload section after font dropdown:
```typescript
{fontFamily === 'custom' && (
  <div className="space-y-2">
    <Label>폰트 파일 업로드</Label>
    <Input 
      type="file" 
      accept=".ttf,.otf,.woff,.woff2"
      onChange={handleFontUpload}
    />
    {customFontUrl && (
      <p className="text-sm text-muted-foreground">
        업로드됨: {customFontUrl}
      </p>
    )}
  </div>
)}
```

Add handler:
```typescript
const handleFontUpload = async (e: React.ChangeEvent<HTMLInputElement>) => {
  const file = e.target.files?.[0];
  if (!file) return;
  const formData = new FormData();
  formData.append('font', file);
  const res = await fetch('/api/fonts', {
    method: 'POST',
    body: formData,
  });
  const data = await res.json();
  setCustomFontUrl(data.fontUrl);
};
```</action>
  <verify>
    <automated>npm run build 2>&1 | head -30</automated>
  </verify>
  <done>StyleEditor has font upload option when "커스텀 폰트" selected</done>
</task>

</tasks>

<verification>
- /api/fonts route accepts font uploads
- fonts.ts supports custom font loading
- StyleEditor has custom font upload UI
</verification>

<success_criteria>
- 사용자가 .ttf 파일 업로드 시 초대장에 해당 폰트 적용됨
</success_criteria>

<output>
After completion, create `.planning/phases/11-custom-fonts/{phase}-02-SUMMARY.md`
</output>