---
phase: 11-custom-fonts
plan: 02
type: execute
wave: 1
depends_on: [11-01]
files_modified:
  - src/app/api/fonts/route.ts
  - src/components/templates/editor/StyleEditor.tsx
  - src/components/publish/InvitationViewer.tsx
  - src/lib/fonts.ts
autonomous: true
requirements: [FONT-03]
user_setup:
  - service: supabase
    why: "custom-fonts Storage bucket 생성 필요"
    env_vars: []
    dashboard_config:
      - task: "Supabase Dashboard → Storage → New Bucket → 'custom-fonts' 생성 (public: true)"
      - task: "또는 API에서 bucket auto-create (현재 코드 패턴 참고)"

must_haves:
  truths:
    - "사용자가 .ttf/.otf/.woff/.woff2 폰트 파일을 업로드할 수 있다"
    - "업로드한 폰트가 최대 5개까지 템플릿에 저장된다"
    - "업로드한 커스텀 폰트가 공개 초대장에 적용된다"
    - "업로드한 폰트를 제거할 수 있다"
  artifacts:
    - path: "src/app/api/fonts/route.ts"
      provides: "커스텀 폰트 업로드/삭제 API"
      exports: ["POST", "DELETE"]
    - path: "src/components/templates/editor/StyleEditor.tsx"
      provides: "폰트 업로드 UI + 상태 관리"
      min_lines: 420
    - path: "src/components/publish/InvitationViewer.tsx"
      provides: "커스텀 폰트 CSS 적용"
  key_links:
    - from: "StyleEditor handleFontUpload"
      to: "/api/fonts POST"
      via: "FormData(font, templateId, fontName)"
      pattern: "POST.*api/fonts"
    - from: "StyleEditor handleRemoveFont"
      to: "/api/fonts DELETE"
      via: "query param url"
      pattern: "DELETE.*api/fonts\\?url="
    - from: "InvitationViewer"
      to: "loadCustomFontCSS"
      via: "customFonts 배열에서 CSS 생성 → inline style"
      pattern: "loadCustomFontCSS.*customFonts"
---

<objective>
사용자가 자신의 커스텀 폰트(.ttf, .otf, .woff, .woff2)를 업로드하고 템플릿에 적용할 수 있도록 구현한다.

Purpose: Phase 09에서 기본 제공 폰트 외에 사용자가 직접 폰트를 업로드하여 고유한 초대장을 만들 수 있게 함. D-01~D-10 결정사항 모두 구현.

Output: 커스텀 폰트 업로드 API + StyleEditor 연동 + InvitationViewer 커스텀 폰트 적용
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/app/api/templates/media/route.ts — Supabase Storage upload/delete 패턴 gold standard
@src/lib/fonts.ts — CustomFont interface, getCustomFontCSS(), loadCustomFontCSS() 이미 구현됨
@src/lib/auth.ts — getUserFromRequest() 헬퍼
@src/components/templates/editor/StyleEditor.tsx — 수정 대상 (fontFamily dropdown 존재, custom 옵션 미포함)
@src/components/publish/InvitationViewer.tsx — 커스텀 폰트 CSS 적용 추가 필요
@src/lib/supabase/database.types.ts — Template config JSONB 필드 (customFonts 저장용)

From media/route.ts (upload pattern):
```typescript
// Supabase Storage upload:
const supabase = createClient(supabaseUrl, supabaseServiceKey);
const { data, error } = await supabase.storage
  .from(bucketName)
  .upload(fileName, buffer, { contentType: file.type, upsert: true });
const { data: urlData } = supabase.storage.from(bucketName).getPublicUrl(fileName);
```

From fonts.ts (already exists):
```typescript
export interface CustomFont {
  id: string;
  name: string;
  url: string;
  family: string;
}
export function getCustomFontCSS(fontUrl: string, fontName: string): string { ... }
export function loadCustomFontCSS(fonts: CustomFont[]): string { ... }
```

From StyleEditor (current fontFamilyOptions):
```typescript
const fontFamilyOptions = [
  { value: 'inherit', label: '기본값' },
  { value: 'Pretendard', label: 'Pretendard' },
  // ... 4 more Google Fonts
];
// NOTE: 'custom' option NOT included yet — must add
```

From InvitationViewer (current font handling):
```typescript
const fontFamily = (layout as Record<string, string>).fontFamily || 'inherit';
// ... Card has style={{ fontFamily: getFontFamily(fontFamily) }}
// NOTE: customFonts from layout NOT loaded yet — must add
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create /api/fonts endpoint (POST + DELETE)</name>
  <files>src/app/api/fonts/route.ts</files>
  <behavior>
    - Test 1: POST without auth returns 401
    - Test 2: POST with invalid file type returns 400
    - Test 3: POST with file > 5MB returns 400
    - Test 4: POST with valid file returns 200 + { success, fontUrl, fontFamily }
    - Test 5: DELETE without url param returns 400
    - Test 6: DELETE with valid url returns 200 + { success }
  </behavior>
  <action>Create src/app/api/fonts/route.ts implementing POST and DELETE handlers.

**POST /api/fonts** — Upload custom font:
```typescript
export async function POST(request: NextRequest) {
  // 1. Auth check (MANDATORY — no dev fallback per D-07)
  const user = await getUserFromRequest(request);
  if (!user) {
    return NextResponse.json({ error: '인증이 필요합니다.' }, { status: 401 });
  }

  // 2. Parse FormData
  const formData = await request.formData();
  const file = formData.get('file') as File | null;
  const templateId = formData.get('templateId') as string;
  const fontName = formData.get('fontName') as string | null; // optional override per D-04

  // 3. Validate file exists
  if (!file) {
    return NextResponse.json({ error: '폰트 파일이 필요합니다.' }, { status: 400 });
  }

  // 4. Validate file type (D-08) — double check extension + MIME
  const allowedExtensions = ['ttf', 'otf', 'woff', 'woff2'];
  const ext = file.name.split('.').pop()?.toLowerCase();
  if (!ext || !allowedExtensions.includes(ext)) {
    return NextResponse.json({ error: '.ttf, .otf, .woff, .woff2 파일만 지원됩니다.' }, { status: 400 });
  }

  // 5. Validate file size (D-02) — 5MB
  const maxSize = 5 * 1024 * 1024;
  if (file.size > maxSize) {
    return NextResponse.json({ error: '최대 5MB까지 업로드 가능합니다.' }, { status: 400 });
  }

  // 6. Determine fontFamily (D-04) — use fontName override or derive from filename
  const family = fontName || sanitizeFileName(file.name);
  // sanitizeFileName: remove extension, replace -/_ with spaces, title case
  // e.g., 'my-wedding-font.ttf' → 'My Wedding Font'

  // 7. Upload to Supabase Storage bucket 'custom-fonts' (D-02)
  const supabase = createClient(supabaseUrl, supabaseServiceKey);
  const bucketName = 'custom-fonts';

  // Auto-create bucket if missing
  const { data: buckets } = await supabase.storage.listBuckets();
  if (!buckets?.find(b => b.name === bucketName)) {
    await supabase.storage.createBucket(bucketName, {
      public: true,
      fileSizeLimit: 5242880, // 5MB
    });
  }

  // Generate unique filename
  const fileId = crypto.randomUUID();
  const fileName = `${user.id}/${templateId}/${fileId}.${ext}`;
  const arrayBuffer = await file.arrayBuffer();
  const buffer = Buffer.from(arrayBuffer);

  const { data: uploadData, error: uploadError } = await supabase.storage
    .from(bucketName)
    .upload(fileName, buffer, { contentType: file.type, upsert: true });

  if (uploadError) {
    console.error('Font upload error:', uploadError);
    return NextResponse.json({ error: '폰트 업로드에 실패했습니다.' }, { status: 500 });
  }

  // 8. Get public URL
  const { data: urlData } = supabase.storage.from(bucketName).getPublicUrl(fileName);

  // 9. Return response
  return NextResponse.json({
    success: true,
    fontUrl: urlData.publicUrl,
    fontFamily: family,
    fontId: fileId,
  });
}
```

**DELETE /api/fonts** — Remove custom font:
```typescript
export async function DELETE(request: NextRequest) {
  // 1. Auth check (MANDATORY)
  const user = await getUserFromRequest(request);
  if (!user) {
    return NextResponse.json({ error: '인증이 필요합니다.' }, { status: 401 });
  }

  // 2. Get url from query params
  const { searchParams } = new URL(request.url);
  const url = searchParams.get('url');
  if (!url) {
    return NextResponse.json({ error: 'url 파라미터가 필요합니다.' }, { status: 400 });
  }

  // 3. Extract file path from URL
  const urlParts = url.split('/storage/v1/object/public/');
  if (urlParts.length < 2) {
    return NextResponse.json({ error: '잘못된 URL 형식입니다.' }, { status: 400 });
  }
  const filePath = urlParts[1];

  // 4. Delete from storage
  const supabase = createClient(supabaseUrl, supabaseServiceKey);
  const { error } = await supabase.storage.from('custom-fonts').remove([filePath]);

  if (error) {
    console.error('Font delete error:', error);
    return NextResponse.json({ error: '파일 삭제에 실패했습니다.' }, { status: 500 });
  }

  return NextResponse.json({ success: true });
}
```

**Helper function** — sanitizeFileName:
```typescript
function sanitizeFileName(filename: string): string {
  // Remove extension
  const name = filename.replace(/\.[^/.]+$/, '');
  // Replace - and _ with spaces
  const spaced = name.replace(/[-_]/g, ' ');
  // Title case
  return spaced.split(' ').map(w => w.charAt(0).toUpperCase() + w.slice(1)).join(' ');
}
```

**File ownership:** This is a new file — no overlap with other tasks.
</action>
  <verify>
    <automated>npm run type-check 2>&1 | grep -E "(Error|error)" | head -10</automated>
  </verify>
  <done>/api/fonts route exists with POST (upload) and DELETE (remove) handlers, auth mandatory, 5MB limit, file type validation</done>
</task>

<task type="auto">
  <name>Task 2: Update StyleEditor with custom font upload UI</name>
  <files>src/components/templates/editor/StyleEditor.tsx</files>
  <action>Update src/components/templates/editor/StyleEditor.tsx:

**1. Add CustomFont import and state:**
```typescript
import { CustomFont } from '@/lib/fonts';

interface StyleEditorProps {
  style: TemplateStyle;
  onChange: (style: TemplateStyle) => void;
  templateId?: string; // NEW — needed for API upload
  customFonts?: CustomFont[]; // NEW — loaded from template config
}
```

**2. Add 'custom' option to fontFamilyOptions (D-06):**
```typescript
const fontFamilyOptions = [
  { value: 'inherit', label: '기본값' },
  { value: 'Pretendard', label: 'Pretendard' },
  { value: 'Noto Sans KR', label: 'Noto Sans KR' },
  { value: 'Nanum Gothic', label: 'Nanum Gothic' },
  { value: 'Open Sans', label: 'Open Sans' },
  { value: 'Playfair Display', label: 'Playfair Display' },
  { value: 'custom', label: '커스텀 폰트' }, // D-06: custom option
];
```

**3. Add state and handlers in StyleEditor component:**
```typescript
export function StyleEditor({ style, onChange, templateId, customFonts = [] }: StyleEditorProps) {
  const [uploading, setUploading] = useState(false);
  const [uploadError, setUploadError] = useState<string | null>(null);

  const handleFontUpload = async (e: React.ChangeEvent<HTMLInputElement>) => {
    const file = e.target.files?.[0];
    if (!file || !templateId) return;

    // D-03: Max 5 fonts per template
    if (customFonts.length >= 5) {
      setUploadError('템플릿당 최대 5개까지 업로드 가능합니다.');
      return;
    }

    setUploading(true);
    setUploadError(null);

    const formData = new FormData();
    formData.append('file', file);
    formData.append('templateId', templateId);
    // D-04: fontName optional override from the text input below
    const customFontName = (style as Record<string, string>).customFontName;
    if (customFontName) {
      formData.append('fontName', customFontName);
    }

    try {
      const res = await fetch('/api/fonts', {
        method: 'POST',
        body: formData,
      });
      const data = await res.json();

      if (!res.ok) {
        setUploadError(data.error || '업로드에 실패했습니다.');
        return;
      }

      // Update customFonts in style config (D-01, D-10)
      const existingFonts = (style as Record<string, CustomFont[]>).customFonts || [];
      const newFont: CustomFont = {
        id: data.fontId,
        name: data.fontFamily,
        url: data.fontUrl,
        family: data.fontFamily,
      };
      onChange({
        ...style,
        customFonts: [...existingFonts, newFont],
      });
    } catch {
      setUploadError('서버 오류가 발생했습니다.');
    } finally {
      setUploading(false);
    }
  };

  const handleRemoveFont = async (font: CustomFont) => {
    try {
      const res = await fetch(`/api/fonts?url=${encodeURIComponent(font.url)}`, {
        method: 'DELETE',
      });

      if (res.ok) {
        const existingFonts = (style as Record<string, CustomFont[]>).customFonts || [];
        onChange({
          ...style,
          customFonts: existingFonts.filter(f => f.id !== font.id),
        });
      }
    } catch {
      setUploadError('삭제에 실패했습니다.');
    }
  };

  // ... rest of component
}
```

**4. Add custom font upload section after fontFamily dropdown (D-06):**
```typescript
{/* 폰트 패밀리 */}
<div className="space-y-2">
  <label className="block text-sm font-medium text-gray-700 dark:text-gray-300">
    폰트
  </label>
  <Select
    value={style.fontFamily || 'inherit'}
    onValueChange={(value) => handleChange('fontFamily', value)}
  >
    <SelectTrigger>
      <SelectValue placeholder="폰트 선택" />
    </SelectTrigger>
    <SelectContent>
      {fontFamilyOptions.map((opt) => (
        <SelectItem key={opt.value} value={opt.value}>
          {opt.label}
        </SelectItem>
      ))}
    </SelectContent>
  </Select>
</div>

{/* D-06: Custom font upload section (conditionally rendered) */}
{(style.fontFamily === 'custom') && (
  <div className="space-y-3 border-t pt-3">
    <label className="block text-sm font-medium text-gray-700 dark:text-gray-300">
      커스텀 폰트 업로드
    </label>

    {/* D-04: Font name override input */}
    <Input
      type="text"
      placeholder="폰트 이름 (선택사항)"
      value={(style as Record<string, string>).customFontName || ''}
      onChange={(e) => handleChange('customFontName', e.target.value)}
      className="text-sm"
    />
    <p className="text-xs text-gray-500">
      미입력 시 파일명에서 자동 파생됩니다
    </p>

    {/* File input */}
    <Input
      type="file"
      accept=".ttf,.otf,.woff,.woff2"
      onChange={handleFontUpload}
      disabled={uploading}
      className="text-sm"
    />
    <p className="text-xs text-gray-500">
      .ttf, .otf, .woff, .woff2 (최대 5MB)
    </p>

    {/* Upload status */}
    {uploading && <p className="text-sm text-blue-600">업로드 중...</p>}
    {uploadError && <p className="text-sm text-red-600">{uploadError}</p>}

    {/* Uploaded fonts list */}
    {customFonts.length > 0 && (
      <div className="space-y-2 mt-2">
        <p className="text-xs font-medium text-gray-600">업로드된 폰트 ({customFonts.length}/5)</p>
        {customFonts.map((font) => (
          <div key={font.id} className="flex items-center justify-between p-2 bg-gray-50 rounded text-sm">
            <span className="truncate flex-1">{font.name}</span>
            <button
              onClick={() => handleRemoveFont(font)}
              className="ml-2 text-red-500 hover:text-red-700 text-xs"
            >
              제거
            </button>
          </div>
        ))}
      </div>
    )}
  </div>
)}
```

**5. Add missing imports:**
```typescript
import { useState } from 'react';
import { Input } from '@/components/ui/input';
import { CustomFont } from '@/lib/fonts';
```

**Note:** The component already imports `Input` and `Select` from shadcn/ui. Add `useState` and `CustomFont` imports.
</action>
  <verify>
    <automated>npm run type-check 2>&1 | grep -E "(Error|error)" | head -10</automated>
  </verify>
  <done>StyleEditor has custom font upload UI, state management, upload/remove handlers, 5-font limit, font name override</done>
</task>

<task type="auto">
  <name>Task 3: Apply custom fonts in InvitationViewer</name>
  <files>src/components/publish/InvitationViewer.tsx</files>
  <action>Update src/components/publish/InvitationViewer.tsx to load and apply custom fonts.

**1. Add imports:**
```typescript
import { getFontFamily, loadCustomFontCSS, CustomFont } from '@/lib/fonts';
```

**2. Extract customFonts from layout:**
```typescript
// After existing fontFamily extraction
const customFonts = (layout as Record<string, CustomFont[]>).customFonts || [];
```

**3. Generate custom font CSS and inject into document:**
```typescript
// Add after existing state declarations
const [customFontCSS, setCustomFontCSS] = useState('');

useEffect(() => {
  if (customFonts.length > 0) {
    const css = loadCustomFontCSS(customFonts);
    setCustomFontCSS(css);

    // Inject CSS into document head
    const styleId = 'custom-font-injection';
    let styleEl = document.getElementById(styleId);
    if (!styleEl) {
      styleEl = document.createElement('style');
      styleEl.id = styleId;
      document.head.appendChild(styleEl);
    }
    styleEl.textContent = css;

    return () => {
      // Cleanup on unmount
      if (styleEl && styleEl.parentNode) {
        styleEl.parentNode.removeChild(styleEl);
      }
    };
  }
}, [customFonts]);
```

**4. Apply custom font as fallback in Card style:**
```typescript
// Update the Card style to use custom font when fontFamily === 'custom'
const effectiveFontFamily = fontFamily === 'custom' && customFonts.length > 0
  ? `'${customFonts[0].family}', sans-serif`
  : getFontFamily(fontFamily);

// In the Card:
<Card className="shadow-lg border-0" style={{ fontFamily: effectiveFontFamily }}>
```

**5. Add custom font CSS variable for CSS-based fallback:**
```typescript
// Add to the outer div style
<div
  className="relative"
  style={customFonts.length > 0 ? {
    '--font-custom': `'${customFonts[0].family}', sans-serif`,
  } : {}}
>
```

This ensures the CSS variable `--font-custom` is set when custom fonts are present, which the fonts.ts `getFontFamily('custom')` already returns as `var(--font-custom)`.
</action>
  <verify>
    <automated>npm run type-check 2>&1 | grep -E "(Error|error)" | head -10</automated>
  </verify>
  <done>InvitationViewer loads customFonts from layout, injects @font-face CSS, applies custom font to Card</done>
</task>

</tasks>

<verification>
- /api/fonts accepts font uploads (POST) and deletions (DELETE) with auth mandatory
- StyleEditor shows custom font upload section when fontFamily === 'custom'
- StyleEditor enforces 5-font limit per template (D-03)
- StyleEditor supports font name override (D-04)
- InvitationViewer loads customFonts and applies @font-face CSS
- Custom font renders in public invitation page
</verification>

<success_criteria>
- StyleEditor에서 "커스텀 폰트" 선택 시 파일 업로드 섹션 표시됨
- .ttf 파일 업로드 시 /api/fonts가 200 + { success, fontUrl, fontFamily } 반환
- 업로드한 폰트가 최대 5개까지 저장되고 제거 버튼으로 삭제 가능
- 공개 초대장 페이지에서 업로드한 커스텀 폰트가 적용됨 (개발자 도구로 font-family 확인 가능)
</success_criteria>

<output>
After completion, create `.planning/phases/11-custom-fonts/{phase}-02-SUMMARY.md`
</output>
