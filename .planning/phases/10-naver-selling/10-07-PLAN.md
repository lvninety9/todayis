---
phase: 10-naver-selling
plan: 07
type: execute
wave: 1
depends_on: [10-01]
files_modified:
  - src/app/(main)/[username]/page.tsx
  - src/components/publish/InvitationViewer.tsx
  - src/components/publish/ShareButton.tsx
autonomous: true
requirements:
  - UI-04
user_setup: []

must_haves:
  truths:
    - wedding 특수 romantic design 적용
    - soft gradients, elegant fonts
    - 모바일 최적화 - 터치 인터랙션
    - 공유 버튼 - modern floating action button
  artifacts:
    - path: src/app/(main)/[username]/page.tsx
      provides: wedding-style public invitation
      min_lines: 150
  key_links:
    - from: InvitationViewer.tsx
      to: ShareButton
      via: FAB
      pattern: floating|share
---

<objective>
공개 초대장 페이지 wedding romantic design 적용

Purpose: wedding 특수 romantic design, 모바일 최적화, 공유 FAB
Output: wedding 스타일 공개 초대장
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-naver-selling/10-CONTEXT.md

Current public invitation page structure:
- InvitationViewer component
- ShareButton component
- ShareDialog component

Reference D-21 to D-23 from CONTEXT.md:
- D-21: wedding特有的 romantic design — soft gradients, elegant fonts
- D-22: 모바일 최적화 — 터치 인터랙션
- D-23: 공유按钮 — modern floating action button
</context>

<tasks>

<task type="auto">
<name>Task 1: Apply romantic wedding design to public page</name>
  <files>src/app/(main)/[username]/page.tsx</files>
<action>
Apply wedding romantic design:

1. Update background:
   - Add soft gradient (pink/purple/cream)
   - Add subtle pattern or texture
   - Consider wedding-appropriate colors

2. Update container styling:
   - Add soft shadows
   - Use elegant rounded corners
   - Add subtle animations

3. Add entrance animation:
   - Fade in with scale
   - Staggered sections

4. Consider adding subtle floral motifs (optional)

Reference D-21 from CONTEXT.md
</action>
<verify>
    <automated>npm run type-check 2>&1 | head -20</automated>
</verify>
<done>Romantic wedding design applied</done>
</task>

<task type="auto">
<name>Task 2: Optimize for mobile with touch interactions</name>
  <files>src/components/publish/InvitationViewer.tsx</files>
<action>
Optimize for mobile:

1. Improve touch scrolling:
   - Smooth scroll between sections
   - Snap to section centers

2. Add touch gestures:
   - Swipe left/right for navigation
   - Tap to zoom on images

3. Make text legible:
   - Appropriate font sizes for mobile
   - Adequate line spacing
   - Dark enough text on backgrounds

Reference D-22 from CONTEXT.md
</action>
<verify>
    <automated>npm run type-check 2>&1 | head -20</automated>
</verify>
<done>Mobile touch optimizations working</done>
</task>

<task type="auto">
<name>Task 3: Make ShareButton a modern FAB</name>
  <files>src/components/publish/ShareButton.tsx</files>
<action>
Redesign ShareButton as FAB:

1. Change to floating action button:
   - Fixed position (bottom-right)
   - Circular shape (w-14 h-14)
   - Drop shadow

2. Update icon:
   - Share icon in center
   - Subtle gradient background

3. Add animations:
   - Scale up on hover
   - Subtle pulse when idle
   - Expand on click

4. Make accessible:
   - Proper label for screen readers
   - Focus states

Reference D-23 from CONTEXT.md
</action>
<verify>
    <automated>npm run type-check 2>&1 | head -20</automated>
</verify>
<done>ShareButton is modern FAB</done>
</task>

</tasks>

<verification>
- [ ] Romantic wedding design applied
- [ ] Mobile touch interactions working
- [ ] Share button is FAB
- [ ] Type-check passes
</verification>

<success_criteria>
Public invitation page has romantic wedding design + mobile optimization + FAB share button
</success_criteria>

<output>
After completion, create `.planning/phases/10-naver-selling/{phase}-07-SUMMARY.md`
</output>