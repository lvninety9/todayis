---
phase: 10-naver-selling
plan: 05
type: execute
wave: 1
depends_on: [10-01]
files_modified:
  - src/app/(main)/templates/[id]/page.tsx
autonomous: true
requirements:
  - NP-02
user_setup: []

must_haves:
  truths:
    - 템플릿 미리보기에 큰 프리뷰 + smooth zoom 적용
    - 구매 버튼에 gradient + glow + pulse animation 적용
    - 정보 섹션에 glassmorphism 카드 적용
  artifacts:
    - path: src/app/(main)/templates/[id]/page.tsx
      provides: modern template detail page
      min_lines: 350
  key_links:
    - from: templates/[id]/page.tsx
      to: Button
      via: purchase button
      pattern: gradient|pulse
---

<objective>
템플릿 상세 페이지 modern design + 네이버 연동

Purpose: 큰 프리뷰, gradient 구매 버튼, glassmorphism 카드
Output: 템플릿 상세 modern UI + 구매 버튼 → 네이버 redirect
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-naver-selling/10-CONTEXT.md
@src/components/ui/button.tsx
@src/components/ui/card.tsx

Current template detail page structure:
- Back link
- Thumbnail image (h-64)
- Template info (name, category, price)
- Button (Edit/Purchase)
- EasyCheckout modal
</context>

<tasks>

<task type="auto">
<name>Task 1: Modernize TemplateDetail with large preview + glass cards</name>
  <files>src/app/(main)/templates/[id]/page.tsx</files>
  <action>
Modernize template detail page:

1. Increase preview size (h-80 or full-width)
   - Add smooth zoom on hover
   - Add click-to-expand modal

2. Apply glassmorphism cards to info sections:
   - Name + category section
   - Price section
   - Action buttons section

3. Add gradient background

4. Add @formkit/auto-animate for sections

Reference D-13, D-14, D-15 from CONTEXT.md

Note: This uses modern Button with gradient variant
</action>
<verify>
    <automated>npm run type-check 2>&1 | head -20</automated>
</verify>
<done>Template detail has large preview + glassmorphism cards</done>
</task>

<task type="auto">
<name>Task 2: Change Purchase button to Naver redirect</name>
  <files>src/app/(main)/templates/[id]/page.tsx</files>
  <action>
Change purchase flow:

1. Update handleBuy function:
   - Remove Toss EasyCheckout integration
   - Instead, redirect to Naver Selling Page
   - Use window.location.href or router.push

2. Create Naver redirect logic:
   - Build Naver Selling Page URL with template info
   - Include return URL for after purchase

3. Update button styling:
   - Use Button variant="gradient"
   - Add pulse animation for attention
   - Label: "{template.name} 구매하기 ({price}원)"

Reference D-31, D-32 from CONTEXT.md

Note: This plan focuses on UI. Naver integration library can be in Plan 10-01.
</action>
<verify>
    <automated>npm run type-check 2>&1 | head -20</automated>
</verify>
<done>Purchase button redirects to Naver Selling Page</done>
</task>

</tasks>

<verification>
- [ ] TemplateDetail has large preview with zoom
- [ ] Info sections use glassmorphism
- [ ] Purchase button uses gradient + pulse
- [ ] Purchase redirects to Naver (or placeholder URL)
- [ ] Type-check passes
</verification>

<success_criteria>
Template detail modernized + Naver redirect purchase flow
</success_criteria>

<output>
After completion, create `.planning/phases/10-naver-selling/{phase}-05-SUMMARY.md`
</output>