---
phase: 10-naver-selling
plan: 10
type: execute
wave: 2
depends_on: [10-01, 10-02, 10-03]
files_modified:
  - src/app/(main)/landing/page.tsx
  - src/app/(main)/templates/detail/[id]/page.tsx
  - src/app/(main)/member/page.tsx
  - src/app/(main)/order-guide/page.tsx
  - src/app/(main)/pricing/page.tsx
autonomous: true
requirements:
  - NP-03
  - NP-04
  - NP-05
  - NP-06
  - NP-07
user_setup: []

must_haves:
  truths:
    - /landing - 메인 랜딩 페이지 (modern gradient + glassmorphism)
    - /templates/detail/[id] - 템플릿 상세 소개 페이지
    - /member - 회원관리 페이지
    - /order-guide - 주문제작 안내 페이지
    - /pricing - 가격 안내 페이지
  artifacts:
    - path: src/app/(main)/landing/page.tsx
      provides: landing page
      min_lines: 150
  key_links:
    - from: landing/page.tsx
      to: templates/detail
      via: routes
      pattern: href|templates
---

<objective>
5개 신규 페이지 추가

Purpose: /landing, /templates/detail/[id], /member, /order-guide, /pricing
Output: 5개 신규 페이지
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-naver-selling/10-CONTEXT.md

Reference D-33 to D-37 from CONTEXT.md:
- D-33: /landing — 메인 랜딩 페이지 (modern gradient + glassmorphism)
- D-34: /templates/detail/[id] — 템플릿 상세 소개 페이지
- D-35: /member — 회원관리 페이지
- D-36: /order-guide — 주문제작 안내 페이지
- D-37: /pricing — 가격 안내 페이지
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Landing page</name>
  <files>src/app/(main)/landing/page.tsx</files>
<action>
Create landing page:

1. Create directory: src/app/(main)/landing/
2. Create page.tsx with:
   - Hero section with gradient + glassmorphism
   - Tagline + CTA buttons
   - Feature highlights (3-4 cards)
   - Template showcase (carousel or grid)
   - Testimonials (optional)
   - Footer with links

3. Apply modern design:
   - GlassCard for feature cards
   - Gradient CTA buttons
   - Smooth animations

Reference D-33 from CONTEXT.md
</action>
<verify>
    <automated>npm run type-check 2>&1 | head -20</automated>
  </verify>
  <done>Landing page created</done>
</task>

<task type="auto">
<name>Task 2: Create Template Detail page</name>
<files>src/app/(main)/templates/detail/[id]/page.tsx</files>
<action>
Create template detail page:

1. Create directory: src/app/(main)/templates/detail/[id]/
2. Create page.tsx with:
   - Large preview (primary focus)
   - Template info (name, category, description)
   - Feature list (editable fields, styles)
   - Preview images/carousel
   - CTA: "이 템플릿으로 만들기"
   - Similar templates section

3. Apply glassmorphism cards

Reference D-34 from CONTEXT.md

Note: Different from /templates/[id] which is for editing. This is for viewing template info before creating.
</action>
<verify>
    <automated>npm run type-check 2>&1 | head -20</automated>
  </verify>
  <done>Template detail page created</done>
</task>

<task type="auto">
<name>Task 3: Create Member management page</name>
<files>src/app/(main)/member/page.tsx</files>
<action>
Create member management page:

1. Create directory: src/app/(main)/member/
2. Create page.tsx with:
   - Member list table
   - Search/filter
   - Role management (admin/user)
   - Status toggle (active/inactive)

3. Use modern design:
   - Card-based layout
   - Table with hover states
   - Action buttons with gradient

Reference D-35 from CONTEXT.md

Note: Likely requires admin role check.
</action>
<verify>
    <automated>npm run type-check 2>&1 | head -20</automated>
  </verify>
  <done>Member management page created</done>
</task>

<task type="auto">
<name>Task 4: Create Order Guide page</name>
<files>src/app/(main)/order-guide/page.tsx</files>
<action>
Create order guide page:

1. Create directory: src/app/(main)/order-guide/
2. Create page.tsx with:
   - Step-by-step guide (1-2-3-4)
   - Each step in glassmorphism card
   - Visual icons for each step
   - FAQ section (optional)

3. Apply modern design:
   - Numbered steps
   - Timeline or card layout

Reference D-36 from CONTEXT.md
</action>
<verify>
    <automated>npm run type-check 2>&1 | head -20</automated>
  </verify>
  <done>Order guide page created</done>
</task>

<task type="auto">
<name>Task 5: Create Pricing page</name>
<files>src/app/(main)/pricing/page.tsx</files>
<action>
Create pricing page:

1. Create directory: src/app/(main)/pricing/
2. Create page.tsx with:
   - Pricing tiers (Free, Basic, Premium)
   - Each tier in glassmorphism card
   - Feature comparison
   - CTA buttons
   - FAQ section

3. Apply modern design:
   - Highlight recommended tier
   - Gradient CTA buttons
   - Check icons for features

Reference D-37 from CONTEXT.md
</action>
<verify>
    <automated>npm run type-check 2>&1 | head -20</automated>
  </verify>
  <done>Pricing page created</done>
</task>

<task type="auto">
  <name>Task 6: Fix back button bug</name>
  <files>src/app/(main)/templates/[id]/page.tsx</files>
<action>
Fix back button bug:

1. Fix navigation: Use Link component instead of button for back
2. In template detail page, update back link:
   ```jsx
   <Link href="/templates" className="...">
     ← 템플릿 라이브러리
   </Link>
   ```

Reference D-39 from CONTEXT.md
</action>
<verify>
    <automated>npm run type-check 2>&1 | head -20</automated>
  </verify>
  <done>Back button fixed</done>
</task>

</tasks>

<verification>
- [ ] /landing page created with modern design
- [ ] /templates/detail/[id] page created
- [ ] /member page created
- [ ] /order-guide page created
- [ ] /pricing page created
- [ ] Back button bug fixed
- [ ] Type-check passes
</verification>

<success_criteria>
5 new pages created + back button bug fixed
</success_criteria>

<output>
After completion, create `.planning/phases/10-naver-selling/{phase}-10-SUMMARY.md`
</output>