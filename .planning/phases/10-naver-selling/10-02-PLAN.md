---
phase: 10-naver-selling
plan: 02
type: execute
wave: 1
depends_on: [10-01]
files_modified:
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/signup/page.tsx
  - src/components/forms/LoginForm.tsx
  - src/components/forms/SignupForm.tsx
autonomous: true
requirements:
  - UI-01
user_setup: []

must_haves:
  truths:
    - 로그인 페이지에 glassmorphism 카드 적용
    - 소셜 로그인 텍스트 가독성 개선
    - gradient 구매 버튼 + hover glow 효과
  artifacts:
    - path: src/app/(auth)/login/page.tsx
      provides: модернизированный 로그인 페이지
      min_lines: 100
    - path: src/app/(auth)/signup/page.tsx
      provides: модернизированный 회원가입 페이지
      min_lines: 100
  key_links:
    - from: login/page.tsx
      to: GlassCard
      via: glassmorphism wrapper
      pattern: GlassCard
---

<objective>
로그인/회원가입 페이지 Glassmorphism 적용 및 소셜 로그인 가독성 개선

Purpose: 로그인/회원가입 UI 현대화
Output: Glassmorphism 카드 + 가독성 개선된 소셜 로그인
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-naver-selling/10-CONTEXT.md
@src/components/ui/card.tsx
@src/components/ui/button.tsx

From login/page.tsx (current line 69-78):
```typescript
<div className="mt-6">
  <div className="relative">
    <div className="absolute inset-0 flex items-center">
      <div className="w-full border-t border-gray-300 dark:border-gray-600" />
    </div>
    <div className="relative flex justify-center text-sm">
      <span className="px-4 bg-white dark:bg-background text-gray-500 dark:text-gray-400 font-medium">
        또는 소셜 계정으로 로그인
      </span>
    </div>
  </div>
  {/* ... */}
</div>
```

Key issue: bg-white background makes text hard to read on gradient
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modernize Login page with GlassCard</name>
  <files>src/app/(auth)/login/page.tsx</files>
  <action>
Modernize login page with 2026 trends:

1. Replace gradient background wrapper with full glassmorphism:
   - Add GlassCard as main container
   - Keep background gradient behind

2. Fix social login text visibility:
   - Change 'px-4 bg-white' to 'px-4 bg-transparent' 
   - OR use dark semi-transparent background
   - Add text shadow for better readability

3. Apply gradient button styling to main login button

4. Add @formkit/auto-animate for smooth transitions on forgot password toggle

Reference D-05, D-06, D-07 from CONTEXT.md
  </action>
  <verify>
    <automated>npm run type-check 2>&1 | head -20</automated>
  </verify>
  <done>Login page has GlassCard + readable social login text</done>
</task>

<task type="auto">
  <name>Task 2: Modernize Signup page</name>
  <files>src/app/(auth)/signup/page.tsx</files>
  <action>
Modernize signup page:

1. Apply same GlassCard approach as login page
2. Fix social signup text visibility (same as login)
3. Apply gradient button styling
4. Add smooth transitions

If signup page doesn't exist or needs to be created, create similar structure to login page.
  </action>
  <verify>
    <automated>npm run type-check 2>&1 | head -20</automated>
  </verify>
  <done>Signup page has GlassCard + readable social text</done>
</task>

</tasks>

<verification>
- [ ] Login page uses GlassCard container
- [ ] Social login text has readable background
- [ ] Signup page uses GlassCard container
- [ ] Type-check passes
</verification>

<success_criteria>
Login/Signup pages modernized with glassmorphism + readable social login text
</success_criteria>

<output>
After completion, create `.planning/phases/10-naver-selling/{phase}-02-SUMMARY.md`
</output>