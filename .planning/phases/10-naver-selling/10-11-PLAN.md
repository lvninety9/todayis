---
phase: 10-naver-selling
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - 신규 의존성 설치 (@formkit/auto-animate, @dnd-kit/*)
  artifacts:
    - path: package.json
      provides: updated dependencies
  key_links: []
---

<objective>
신규 의존성 설치

Purpose: @formkit/auto-animate 및 @dnd-kit/* 패키지 설치
Output: 신규 의존성 추가
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-naver-selling/10-CONTEXT.md

Required dependencies:
- @formkit/auto-animate: 부드러운 전환 효과 (all lists)
- @dnd-kit/core: 드래그 앤 드롭
- @dnd-kit/sortable: 정렬 가능한 리스트
- @dnd-kit/utilities: 유틸리티 함수
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install new dependencies</name>
  <files>package.json</files>
<action>
Install new dependencies:

1. Run npm install:
   - npm install @formkit/auto-animate
   - npm install @dnd-kit/core
   - npm install @dnd-kit/sortable
   - npm install @dnd-kit/utilities

2. Or add to package.json manually:
   ```json
   "@formkit/auto-animate": "^1.0.0",
   "@dnd-kit/core": "^6.1.0",
   "@dnd-kit/sortable": "^8.0.0",
   "@dnd-kit/utilities": "^3.2.0"
   ```

3. Run npm install to apply

Note: This is Wave 1 because other plans depend on these libraries.
</action>
  <verify>
    <automated>npm list @formkit/auto-animate @dnd-kit/core 2>&1 | head -10</automated>
  </verify>
  <done>New dependencies installed</done>
</task>

</tasks>

<verification>
- [ ] @formkit/auto-animate installed
- [ ] @dnd-kit packages installed
- [ ] npm install succeeds
</verification>

<success_criteria>
New animation and drag-drop dependencies ready
</success_criteria>

<output>
After completion, create `.planning/phases/10-naver-selling/{phase}-11-SUMMARY.md`
</output>