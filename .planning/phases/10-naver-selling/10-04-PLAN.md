---
phase: 10-naver-selling
plan: 04
type: execute
wave: 1
depends_on: [10-01]
files_modified:
  - src/app/(main)/templates/page.tsx
  - src/components/templates/library/TemplateLibrary.tsx
  - src/components/templates/library/TemplateCard.tsx
autonomous: true
requirements:
  - UI-03
user_setup: []

must_haves:
  truths:
    - 템플릿 카드 modern design 적용
    - rounded corners, shadow, hover lift 효과
    - 필터/정렬 UI에 glassmorphism 패널 적용
  artifacts:
    - path: src/components/templates/library/TemplateCard.tsx
      provides: modern template card
      min_lines: 120
  key_links:
    - from: TemplateCard.tsx
      to: GlassCard
      via: card wrapper
      pattern: GlassCard|hover
---

<objective>
템플릿 라이브러리 페이지 modern design 적용

Purpose: 템플릿 카드 modern design, hover lift
Output: 템플릿 라이브러리 modern UI
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-naver-selling/10-CONTEXT.md
@src/components/templates/library/TemplateCard.tsx

From templates/page.tsx:
- TemplateLibrary component with templates list
- TemplateUploadDialog for new templates
</context>

<tasks>

<task type="auto">
<name>Task 1: Modernize TemplateCard with hover lift</name>
<files>src/components/templates/library/TemplateCard.tsx</files>
<action>
Modernize TemplateCard component:

1. Add rounded-xl corners
2. Add subtle shadow (shadow-lg or custom)
3. Add hover lift animation (translate-y-2, scale-102)
4. Add gradient overlay on thumbnail hover
5. Add smooth transition (duration-200)
6. Update thumbnail aspect ratio (16:9 or 4:3)

Reference D-11 from CONTEXT.md
</action>
<verify>
    <automated>npm run type-check 2>&1 | head -20</automated>
</verify>
<done>TemplateCard has modern design + hover lift</done>
</task>

<task type="auto">
<name>Task 2: Modernize TemplateLibrary filter panel</name>
<files>src/components/templates/library/TemplateLibrary.tsx</files>
<action>
Modernize TemplateLibrary filter/search panel:

1. Add glassmorphism background to filter panel
2. Add rounded-xl container
3. Add subtle backdrop blur
4. Add smooth filter transitions

Reference D-12 from CONTEXT.md
</action>
<verify>
    <automated>npm run type-check 2>&1 | head -20</automated>
</verify>
<done>TemplateLibrary has glassmorphism filter panel</done>
</task>

<task type="auto">
<name>Task 3: Add auto-animate for smooth list transitions</name>
<files>src/app/(main)/templates/page.tsx</files>
<action>
Add @formkit/auto-animate for smooth transitions:

1. Import { useAutoAnimate } from @formkit/auto-animate
2. Add to template list container
3. Add to dialog open/close container
4. Add to sorting/filtering
</action>
<verify>
    <automated>npm run type-check 2>&1 | head -20</automated>
</verify>
<done>Smooth list transitions working</done>
</task>

</tasks>

<verification>
- [ ] TemplateCard has rounded-xl + hover lift
- [ ] TemplateLibrary has glassmorphism filter panel
- [ ] Auto-animate smooth transitions work
- [ ] Type-check passes
</verification>

<success_criteria>
Template library modernized with modern cards + glassmorphism + smooth animations
</success_criteria>

<output>
After completion, create `.planning/phases/10-naver-selling/{phase}-04-SUMMARY.md`
</output>