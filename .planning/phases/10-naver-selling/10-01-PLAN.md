---
phase: 10-naver-selling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/ui/input.tsx
  - tailwind.config.ts
  - src/app/globals.css
autonomous: true
requirements:
  - NP-01
  - NP-02
user_setup: []

must_haves:
  truths:
    - 버튼은 gradient background, hover glow, scale animation 적용
    - 입력폼은 glassmorphism 배경, focus glow 적용
    - 카드는 rounded-xl, subtle shadow, hover lift animation 적용
  artifacts:
    - path: src/components/ui/button.tsx
      provides: 모던 버튼 컴포넌트
      min_lines: 60
    - path: src/components/ui/card.tsx
      provides: GlassCard 컴포넌트
      min_lines: 80
    - path: src/components/ui/input.tsx
      provides: 모던 입력폼
      min_lines: 30
  key_links:
    - from: button.tsx
      to: tailwind.config.ts
      via: 커스텀 gradient/glow 설정
      pattern: gradient|shadow|glow
---

<objective>
2026 트렌드 디자인 시스템 적용 - 버튼, 입력폼, 카드 컴포넌트 현대화

Purpose: Glassmorphism 2.0, modern gradient, glow effects 적용
Output: 재사용 가능한 모던 UI 컴포넌트 세트
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-naver-selling/10-CONTEXT.md

From button.tsx (current):
```typescript
const buttonVariants = cva(
  'inline-flex items-center justify-center whitespace-nowrap rounded-md text-sm font-medium ring-offset-background transition-all duration-200 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50 active:scale-95 hover:scale-105',
  {
    variants: {
      variant: {
        default: 'bg-primary text-primary-foreground hover:bg-primary/90',
        // ...
      },
    },
  }
);
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Button component with modern gradient + glow</name>
  <files>src/components/ui/button.tsx</files>
  <action>
Update buttonVariants to include modern gradient styles:

1. Add new variant 'gradient' with gradient background:
   - background: linear-gradient(to right, #6366f1, #8b5cf6)
   - box-shadow on hover: 0 0 20px rgba(99, 102, 241, 0.4)
   - scale on hover: scale-102
   - transition: all 0.2s ease

2. Update default variant:
   - Add subtle gradient
   - Add glow effect on hover

3. Keep existing variants (destructive, outline, secondary, ghost, link)

Reference D-27, D-03 from CONTEXT.md
  </action>
  <verify>
    <automated>npm run type-check 2>&1 | head -20</automated>
  </verify>
  <done>Button component has gradient variant with glow + scale effect</done>
</task>

<task type="auto">
  <name>Task 2: Update Card components with glassmorphism</name>
  <files>src/components/ui/card.tsx</files>
  <action>
Enhance GlassCard component:

1. Add backdrop-filter: blur(20px) for glassmorphism 2.0
2. Add semi-transparent background with subtle white overlay
3. Add soft shadow and border
4. Add hover lift animation (transform: translateY(-2px))
5. Add subtle scale on hover

Keep Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter as-is but update GlassCard.

Reference D-01, D-28 from CONTEXT.md
  </action>
  <verify>
    <automated>npm run type-check 2>&1 | head -20</automated>
  </verify>
  <done>GlassCard has glassmorphism + hover lift animation</done>
</task>

<task type="auto">
  <name>Task 3: Update Input component with glassmorphism</name>
  <files>src/components/ui/input.tsx</files>
  <action>
Enhance Input component:

1. Add glassmorphism background (bg-white/60 or dark:bg-black/40)
2. Add backdrop filter blur
3. Add border glow on focus (ring-2)
4. Keep existing functionality

Reference D-29 from CONTEXT.md
  </action>
  <verify>
    <automated>npm run type-check 2>&1 | head -20</automated>
  </verify>
  <done>Input has glassmorphism background + focus glow</done>
</task>

</tasks>

<verification>
- [ ] Button with gradient + glow + scale animation passes type-check
- [ ] GlassCard with glassmorphism 2.0 + hover lift passes type-check
- [ ] Input with glassmorphism + focus glow passes type-check
</verification>

<success_criteria>
Modern UI components ready: gradient button variant, GlassCard with hover animation, Input with focus glow
</success_criteria>

<output>
After completion, create `.planning/phases/10-naver-selling/{phase}-01-SUMMARY.md`
</output>