---
phase: 10-naver-selling
plan: 03
type: execute
wave: 1
depends_on: [10-01]
files_modified:
  - src/app/(main)/dashboard/page.tsx
autonomous: true
requirements:
  - UI-02
user_setup: []

must_haves:
  truths:
    - 대시보드에 카드 그리드 적용
    - 통계 대시보드 UI 적용
    - 빠른 작업 버튼에 gradient + glow
  artifacts:
    - path: src/app/(main)/dashboard/page.tsx
      provides: modern dashboard page
      min_lines: 250
  key_links:
    - from: dashboard/page.tsx
      to: GlassCard
      via: cards for actions
      pattern: GlassCard
---

<objective>
대시보드 페이지 현대화 - 카드 그리드, 통계 대시보드

Purpose: 대시보드 구조/짜임새 개선, modern card 스타일
Output: 카드 그리드 + 통계 대시보드 UI
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-naver-selling/10-CONTEXT.md
@src/components/ui/card.tsx
@src/components/ui/button.tsx

From dashboard/page.tsx (current structure):
- Quick action cards (Templates, New Invitation, Settings)
- Recent templates list
- Uses GlassCard with hover:scale-[1.02]
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modernize Dashboard with card grid layout</name>
  <files>src/app/(main)/dashboard/page.tsx</files>
  <action>
Modernize dashboard page:

1. Add statistics dashboard section:
   - Templates count card
   - Invitations count card
   - Purchases count card (if e-commerce)
   - Use gradient backgrounds for stat cards

2. Improve card grid:
   - Use 3-column grid on desktop, 1-column on mobile
   - Add hover lift animation
   - Use gradient + glow on icon backgrounds

3. Improve recent templates:
   - Add hover animation
   - Add status badges
   - Add action buttons with gradient

4. Add subtle entrance animations using @formkit/auto-animate

Reference D-08, D-09, D-10 from CONTEXT.md
  </action>
  <verify>
    <automated>npm run type-check 2>&1 | head -20</automated>
  </verify>
  <done>Dashboard has card grid + statistics with modern styling</done>
</task>

</tasks>

<verification>
- [ ] Dashboard uses card grid layout
- [ ] Statistics section added
- [ ] Action buttons use gradient + glow
- [ ] Type-check passes
</verification>

<success_criteria>
Dashboard modernized with card grid + statistics + modern buttons
</success_criteria>

<output>
After completion, create `.planning/phases/10-naver-selling/{phase}-03-SUMMARY.md`
</output>