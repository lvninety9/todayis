---
phase: 10-naver-selling
plan: 09
type: execute
wave: 1
depends_on: [10-01]
files_modified:
  - src/app/(main)/layout.tsx
  - src/app/layout.tsx
autonomous: true
requirements:
  - UI-05
user_setup: []

must_haves:
  truths:
    - 헤더에 glassmorphism + sticky 적용
    - 모바일 네비게이션에 bottom tab bar 적용
    - 푸터에 minimal + gradient accent 적용
  artifacts:
    - path: src/app/(main)/layout.tsx
      provides: main layout with modern nav
      min_lines: 80
  key_links:
    - from: layout.tsx
      to: Header|Footer
      via: components
      pattern: glass|sticky|bottom
---

<objective>
네비게이션/헤더/푸터 glassmorphism 적용

Purpose: 헤더 glassmorphism + sticky, 모바일 bottom tab, 푸터 minimal
Output: 현대적인 네비게이션 시스템
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-naver-selling/10-CONTEXT.md
@src/app/(main)/layout.tsx
@src/app/layout.tsx

Reference D-24 to D-26 from CONTEXT.md:
- D-24: 헤더 — glassmorphism + sticky
- D-25: 모바일 네비게이션 — bottom tab bar
- D-26: 푸터 — minimal with gradient accent
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update MainHeader with glassmorphism</name>
  <files>src/app/(main)/layout.tsx</files>
<action>
Modernize header:

1. Update or create MainHeader component:
   - Add backdrop-filter: blur(20px)
   - Add bg-white/80 or bg-black/40 (semi-transparent)
   - Add sticky position (sticky top-0)
   - Add subtle shadow on scroll

2. Update navigation:
   - Modern styled links
   - Hover underline animation
   - Active state indicator

3. Add mobile hamburger menu:
   - Slide-in drawer

Reference D-24 from CONTEXT.md
</action>
<verify>
    <automated>npm run type-check 2>&1 | head -20</automated>
  </verify>
  <done>Header with glassmorphism + sticky</done>
</task>

<task type="auto">
<name>Task 2: Add mobile bottom tab bar</name>
<files>src/app/(main)/layout.tsx</files>
<action>
Add mobile navigation:

1. Create BottomTab component:
   - Fixed position at bottom
   - Show on mobile only (hidden on desktop)
   - 4-5 tabs: Home, Templates, Create, Profile
   - Active indicator

2. Add responsive visibility:
   - Desktop: Show header, hide bottom tab
   - Mobile: Hide header (or shrink), show bottom tab

3. Use @formkit/auto-animate for smooth transitions

Reference D-25 from CONTEXT.md
</action>
<verify>
    <automated>npm run type-check 2>&1 | head -20</automated>
</verify>
<done>Mobile bottom tab bar working</done>
</task>

<task type="auto">
<name>Task 3: Modernize Footer</name>
<files>src/app/layout.tsx</files>
<action>
Modernize footer:

1. Update Footer styling:
   - Minimal design
   - Gradient accent (small top border or icon)
   - Social links with hover animation

2. Keep functional:
   - Copyright
   - Links to terms/privacy (if needed)

Reference D-26 from CONTEXT.md
</action>
<verify>
    <automated>npm run type-check 2>&1 | head -20</automated>
</verify>
<done>Footer with minimal + gradient accent</done>
</task>

</tasks>

<verification>
- [ ] Header has glassmorphism + sticky
- [ ] Mobile bottom tab bar works
- [ ] Footer has minimal design
- [ ] Type-check passes
</verification>

<success_criteria>
Navigation modernized: glassmorphism header + mobile bottom tabs + minimal footer
</success_criteria>

<output>
After completion, create `.planning/phases/10-naver-selling/{phase}-09-SUMMARY.md`
</output>