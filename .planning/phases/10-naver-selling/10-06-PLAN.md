---
phase: 10-naver-selling
plan: 06
type: execute
wave: 1
depends_on: [10-01]
files_modified:
  - src/components/templates/editor/TemplateEditor.tsx
  - src/components/templates/editor/FieldEditor.tsx
  - src/components/templates/editor/StyleEditor.tsx
  - src/app/(main)/templates/[id]/edit/page.tsx
autonomous: true
requirements:
  - EDITOR-01
  - EDITOR-02
user_setup: []

must_haves:
  truths:
    - 섹션 드래그 앤 드롭으로 위치 이동 가능
    - 섹션별 음악/애니메이션/폰트 설정 UI
    - 실시간 미리보기 강화
    - 도구 모음 단순화
    - 모바일/데스크톱 프리뷰 통합
  artifacts:
    - path: src/components/templates/editor/TemplateEditor.tsx
      provides: modernized template editor
      min_lines: 200
  key_links:
    - from: TemplateEditor.tsx
      to: @dnd-kit/core
      via: drag and drop
      pattern: DndContext|useSortable
---

<objective>
편집기 완전히 재설계 - 드래그 앤 드롭, 실시간 프리뷰, 섹션별 설정

Purpose: 직관적인 드래그 앤 드롭, 섹션별 설정, 실시간 미리보기
Output: 완전히 재설계된 편집기
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-naver-selling/10-CONTEXT.md

Current TemplateEditor structure:
- Left: Field list
- Right: FieldEditor per field
- Bottom: Validate + Save buttons

Reference D-16 to D-20 from CONTEXT.md
- D-16: 직관적인 드래그 앤 드롭 — 섹션 위치 자유 배치
- D-17: 실시간 미리보기 강화 — split view 또는 toggle
- D-18: 섹션별 설정 패널 — 음악, 애니메이션, 폰트 개별 설정
- D-19: 도구 모음 단순화 — 필요할 때만 표시
- D-20: 모바일/데스크톱 프리뷰 통합 — 한 화면에서 전환
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add drag-and-drop with @dnd-kit</name>
  <files>src/components/templates/editor/TemplateEditor.tsx</files>
  <action>
Redesign editor with drag-and-drop:

1. Install @dnd-kit/core and @dnd-kit/sortable
   - Add to package.json if needed

2. Wrap field sections with Sortable:
   - Use DndContext for whole editor
   - Use useSortable hook for each field section
   - Add drag handle (grip icon)

3. Add visual drop zone feedback:
   - Highlight drop position
   - Smooth animation during drag

4. Reorder fields array in state

Reference D-16 from CONTEXT.md
</action>
  <verify>
    <automated>npm run type-check 2>&1 | head -20</automated>
  </verify>
  <done>Drag-and-drop working for field reordering</done>
</task>

<task type="auto">
  <name>Task 2: Add section settings panel</name>
  <files>src/components/templates/editor/StyleEditor.tsx</files>
  <action>
Add per-section settings:

1. Create or enhance StyleEditor:
   - Music selection (from uploaded tracks)
   - Animation style (fade, slide, zoom, none)
   - Font selection (from available fonts)
   - Text alignment

2. Add settings toggle per section:
   - Small gear icon on hover
   - Opens settings panel

3. Store in section-specific config

Reference D-18 from CONTEXT.md
</action>
<verify>
    <automated>npm run type-check 2>&1 | head -20</automated>
  </verify>
  <done>Section settings panel working</done>
</task>

<task type="auto">
  <name>Task 3: Enhance real-time preview</name>
  <files>src/app/(main)/templates/[id]/edit/page.tsx</files>
<action>
Enhance preview system:

1. Add split view option:
   - Left: Editor, Right: Preview
   - Toggle button to show/hide

2. Add mobile/desktop toggle:
   - Buttons to switch preview mode
   - Use iframe or container resize

3. Add live refresh:
   - Update preview on field change (debounced)

Reference D-17, D-20 from CONTEXT.md
</action>
<verify>
    <automated>npm run type-check 2>&1 | head -20</automated>
</verify>
<done>Split view + mobile/desktop preview working</done>
</task>

<task type="auto">
  <name>Task 4: Simplify toolbar</name>
  <files>src/components/templates/editor/TemplateEditor.tsx</files>
<action>
Simplify editor toolbar:

1. Group buttons intelligently:
   - Main actions always visible
   - Secondary options hidden by default
   - Expandable with "+" or "..."

2. Add keyboard shortcuts hints:
   - Show on hover or in tooltip

3. Modernize button styling:
   - Use gradient + glow for primary
   - Use icon buttons where appropriate

Reference D-19 from CONTEXT.md
</action>
<verify>
    <automated>npm run type-check 2>&1 | head -20</automated>
</verify>
<done>Simplified toolbar with expandable options</done>
</task>

</tasks>

<verification>
- [ ] Drag-and-drop reordering works
- [ ] Section settings panel works
- [ ] Split view + mobile/desktop preview works
- [ ] Simplified toolbar
- [ ] Type-check passes
</verification>

<success_criteria>
Editor completely redesigned with drag-and-drop, section settings, enhanced preview
</success_criteria>

<output>
After completion, create `.planning/phases/10-naver-selling/{phase}-06-SUMMARY.md`
</output>