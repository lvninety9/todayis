---
phase: 10-naver-selling
plan: 08
type: execute
wave: 1
depends_on: [10-05]
files_modified:
  - src/lib/payment/naver.ts
  - next.config.js
autonomous: true
requirements:
  - NP-01
user_setup:
  - service: naver
    why: "Payment processing via Naver Selling Page"
    env_vars:
      - name: NAVER_SELLING_PAGE_URL
        source: "Naver Selling Page dashboard"
    dashboard_config:
      - task: "Configure template as product"
        location: "Naver Selling Page admin"

must_haves:
  truths:
    - Naver Selling Page redirect 방식으로 연동
    - 템플릿 상세 페이지 구매 버튼 → 네이버로 redirect
  artifacts:
    - path: src/lib/payment/naver.ts
      provides: Naver payment redirect client
      min_lines: 50
    - path: next.config.js
      provides: image hostname config
      min_lines: 20
  key_links:
    - from: templates/[id]/page.tsx
      to: naver.ts
      via: redirect function
      pattern: window.location
---

<objective>
Naver Selling Page 연동 + image hostname 설정

Purpose:Naver Selling Page redirect 방식 구현
Output: Image hostname config + Naver redirect client
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-naver-selling/10-CONTEXT.md

Reference D-31, D-32, D-38 from CONTEXT.md:
- D-31: Naver Selling Page redirect 방식으로 연동
- D-32: 템플릿 상세 페이지 구매 버튼 → 네이버로 redirect
- D-38: next.config.js — images.pexels.com hostname 추가
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add image hostname to next.config.js</name>
  <files>next.config.js</files>
<action>
Add Pexels hostname:

1. Read current next.config.js
2. Add to images.domains or images.remotePatterns:
   ```javascript
   images: {
     remotePatterns: [
       {
         protocol: 'https',
         hostname: 'images.pexels.com',
         pathname: '/**',
       },
     ],
   }
   ```

Reference D-38 from CONTEXT.md
</action>
<verify>
    <automated>npm run type-check 2>&1 | head -20</automated>
</verify>
  <done>Pexels hostname added</done>
</task>

<task type="auto">
  <name>Task 2: Create Naver payment redirect client</name>
  <files>src/lib/payment/naver.ts</files>
<action>
Create Naver Selling Page client:

1. Create src/lib/payment/naver.ts:
   - Define NaverPaymentClient class/functions
   - Create redirectToNaverSellingPage function:
     - Takes template name, price, return URL
     - Builds Naver Selling Page URL
     - Redirects via window.location.href
   - Include env var for base URL

2. Export for use in template detail page

Reference D-31 from CONTEXT.md
</action>
<verify>
    <automated>npm run type-check 2>&1 | head -20</automated>
</verify>
  <done>Naver redirect client ready</done>
</task>

<task type="auto">
  <name>Task 3: Update template detail to use Naver client</name>
  <files>src/app/(main)/templates/[id]/page.tsx</files>
<action>
Integrate Naver client:

1. Import redirectToNaverSellingPage from naver.ts

2. Update handleBuy:
   - Replace Toss payment request
   - Call redirectToNaverSellingPage with:
     - templateName: template.name
     - price: template.price
     - returnUrl: {current page}/completed

3. Remove EasyCheckout modal usage

Earlier plan 10-05 already updated UI - this ensures proper Naver integration.
</action>
<verify>
    <automated>npm run type-check 2>&1 | head -20</automated>
  </verify>
  <done>Template detail uses Naver redirect</done>
</task>

</tasks>

<verification>
- [ ] Pexels image hostname added
- [ ] Naver redirect client created
- [ ] Template detail integrates Naver client
- [ ] Type-check passes
</verification>

<success_criteria>
Naver Selling Page integration complete + image configs
</success_criteria>

<output>
After completion, create `.planning/phases/10-naver-selling/{phase}-08-SUMMARY.md`
</output>