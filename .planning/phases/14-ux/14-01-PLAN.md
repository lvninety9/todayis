---
phase: 14-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(main)/layout.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - BUG-01
  - BUG-02
must_haves:
  truths:
    - "로고 클릭 시 /landing 페이지로 이동"
    - "Root 페이지(/) 접근 시 /landing으로 리다이렉트"
  artifacts:
    - path: src/app/(main)/layout.tsx
      provides: "메인 레이아웃 — 로고 링크"
      min_lines: 39
    - path: src/app/page.tsx
      provides: "Root redirect"
      min_lines: 4
  key_links:
    - from: src/app/(main)/layout.tsx
      to: src/app/(main)/landing/page.tsx
      via: "Link href='/landing'"
      pattern: "href=\"/landing\""
    - from: src/app/page.tsx
      to: src/app/(main)/landing/page.tsx
      via: "redirect('/landing')"
      pattern: "redirect\\('/landing'\\)"
user_setup: []

---

<objective>
Bug Fixes: 로고 클릭 리다이렉트 및 Root 페이지 리다이렉트 수정

Purpose: Phase 13 디자인 시스템 적용 후 발견된 네비게이션 버그 수정 — 로고 클릭 시 로그인 페이지가 아닌 랜딩 페이지로 이동하도록 통일
Output: layout.tsx의 로고 href 변경, page.tsx의 redirect 대상 변경
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-ux/14-CONTEXT.md

# Current code context
@src/app/(main)/layout.tsx
@src/app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 로고 클릭 시 /landing 리다이렉트 (BUG-01)</name>
  <files>src/app/(main)/layout.tsx</files>
  <action>
    Line 39: 로고 Link의 href를 "/"에서 "/landing"으로 변경

    Before:
    ```tsx
    <Link href="/" className="flex items-center">
    ```

    After:
    ```tsx
    <Link href="/landing" className="flex items-center">
    ```

    Reason: 모든 사용자가 로고를 클릭했을 때 랜딩 페이지로 이동해야 함 (D-01)
    로그인 페이지로 가는 것은 UX 버그 — 메인 페이지 진입은 /landing이 표준 경로
  </action>
  <verify>
    <automated>grep -n 'href="/landing"' src/app/\(main\)/layout.tsx</automated>
  </verify>
  <done>layout.tsx line 39에서 href="/landing" 확인 — 로고 클릭 시 /landing 이동</done>
</task>

<task type="auto">
  <name>Task 2: Root 페이지 / → /landing 리다이렉트 (BUG-02)</name>
  <files>src/app/page.tsx</files>
  <action>
    Line 4: redirect 대상을 "/login"에서 "/landing"으로 변경

    Before:
    ```tsx
    redirect('/login');
    ```

    After:
    ```tsx
    redirect('/landing');
    ```

    Reason: Root 접근 시 로그인 페이지가 아닌 랜딩 페이지로 유도 (D-02)
    로그인 페이지는 /login 경로로 직접 접근하거나 로고 클릭 시 도달
  </action>
  <verify>
    <automated>grep -n "redirect('/landing')" src/app/page.tsx</automated>
  </verify>
  <done>page.tsx에서 redirect('/landing') 확인 — Root 접근 시 /landing 리다이렉트</done>
</task>

</tasks>

<verification>
### Code Verification
- [ ] BUG-01: layout.tsx line 39 `href="/landing"` 확인
- [ ] BUG-02: page.tsx line 4 `redirect('/landing')` 확인
</verification>

<success_criteria>
- 로고 클릭 시 /landing 페이지로 이동 (기존 /login 버그 수정)
- Root 페이지(/) 접근 시 /landing으로 리다이렉트
- `npm run build` 성공
- `npm run lint` 통과
</success_criteria>

<output>
After completion, create `.planning/phases/14-ux/14-01-SUMMARY.md`
</output>
