---
phase: 14-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(main)/dashboard/page.tsx
  - src/app/(main)/landing/page.tsx
  - src/components/ui/card.tsx
autonomous: true
requirements:
  - UX-02
must_haves:
  truths:
    - "Dashboard가 Bento Grid Layout으로 표시"
    - "Landing page가 Bento Grid Layout으로 표시"
    - "각 섹션이 varied sizes로 시각적 계층形成"
  artifacts:
    - path: src/app/(main)/dashboard/page.tsx
      provides: "대시보드 — Bento Grid 통계 카드"
      min_lines: 140
    - path: src/app/(main)/landing/page.tsx
      provides: "랜딩 페이지 — Bento Grid 피처 섹션"
      min_lines: 100
  key_links:
    - from: src/app/(main)/dashboard/page.tsx
      to: src/components/ui/card.tsx
      via: "GlassCard with col-span"
      pattern: "col-span-.*lg:col-span"
user_setup: []

---

<objective>
Bento Grid Layout 구현 — Dashboard 및 Landing Page에 적용

Purpose: 2026년 최신 트렌드인 Bento Grid Layout을 적용하여 시각적 계층과 modern SaaS look实现
Output: Dashboard 통계 카드와 Landing Page 피처 섹션을 asymmetric bento grid로 재구성
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-ux/14-CONTEXT.md

# Design references
@src/app/(main)/dashboard/page.tsx
@src/app/(main)/landing/page.tsx
@src/components/ui/card.tsx

# Reference: shadcn.io Bento Grid pattern
# - Asymmetric grid: lg:col-span-2 (hero) + lg:col-span-1 (supporting)
# - Varied col-span + row-span for visual hierarchy
# - Interactive hover effects per card
# - Responsive: collapse to single column on mobile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard Bento Grid 구현</name>
  <files>src/app/(main)/dashboard/page.tsx</files>
  <read_first>src/app/(main)/dashboard/page.tsx</read_first>
  <action>
    Dashboard의 통계 카드 섹션을 Bento Grid Layout으로 변환:

    1. 현재 Grid (lines 98-140): 3-column equal width statistics cards
    2. 변경 후:
       - Template 카드를 lg:col-span-2 (wide), Invitation/Purchase를 lg:col-span-1 (narrow)
       - Quick Actions 섹션도 asymmetric: "새 초대장 만들기"를 lg:col-span-2, 나머지는 lg:col-span-1

    Before:
    ```tsx
    <div className="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
      <GlassCard className="p-6">...</GlassCard>
      <GlassCard className="p-6">...</GlassCard>
      <GlassCard className="p-6">...</GlassCard>
    </div>
    ```

    After:
    ```tsx
    <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 mb-8">
      <GlassCard className="p-6 lg:col-span-2">...</GlassCard>
      <GlassCard className="p-6">...</GlassCard>
      <GlassCard className="p-6">...</GlassCard>
    </div>
    ```

    Quick Actions도 동일 적용. Hover effects 유지 (hover:-translate-y-1).

    Warm palette 색상 유지 (terracotta/sage/blush gradients).
  </action>
  <acceptance_criteria>
    - [ ] dashboard page에 lg:col-span-2 클래스 적용됨
    - [ ] md:grid-cols-2 lg:grid-cols-3 responsive grid 적용
    - [ ] Template 카드가 2배 너비로 표시 (lg 이상에서)
    - [ ] npm run build 성공
    - [ ] npm run lint 통과
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npm run lint</automated>
  </verify>
  <done>Dashboard가 Bento Grid Layout으로 표시 — Template 카드 lg:col-span-2, 나머지 lg:col-span-1</done>
</task>

<task type="auto">
  <name>Task 2: Landing Page Bento Grid 구현 (optional stretch)</name>
  <files>src/app/(main)/landing/page.tsx</files>
  <read_first>src/app/(main)/landing/page.tsx</read_first>
  <action>
    Landing Page의 피처 섹션을 Bento Grid Layout으로 변환:

    1. 현재: 4-column equal width (line 73: md:grid-cols-2 lg:grid-cols-4)
    2. 변경: Asymmetric grid — Hero feature lg:col-span-2, 나머지는 lg:col-span-1
       - "예쁘고 특별한 초대장"을 hero로 — lg:col-span-2 row-span-2
       - 나머지 3개는 supporting cards
       - CTA 카드도 lg:col-span-2 또는 full width

    Before:
    ```tsx
    <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
      <GlassCard className="p-6 text-center">...</GlassCard>
      ...
    </div>
    ```

    After:
    ```tsx
    <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
      {/* Hero - spans 2 cols, 2 rows */}
      <GlassCard className="p-6 lg:col-span-2 lg:row-span-2">...</GlassCard>
      {/* Supporting */}
      <GlassCard className="p-6">...</GlassCard>
      <GlassCard className="p-6">...</GlassCard>
      <GlassCard className="p-6">...</GlassCard>
    </div>
    ```

    각 카드에 hover-effect: scale-105, shadow-lg 추가.
  </action>
  <acceptance_criteria>
    - [ ] landing page에 lg:col-span-2, lg:row-span-2 적용
    - [ ] Hero 카드가 2x2 크기로 표시
    - [ ] md:grid-cols-2 lg:grid-cols-3 responsive 적용
    - [ ] npm run build 성공
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Landing page가 Bento Grid Layout으로 표시 — Hero 피처가 lg:col-span-2, lg:row-span-2</done>
</task>

</tasks>

<verification>
### Code Verification
- [ ] dashboard/page.tsx에 lg:col-span-2 적용 확인
- [ ] landing/page.tsx에 lg:col-span-2 + lg:row-span-2 적용 확인
- [ ] Warm palette 색상 유지 (hard-coded indigo 없음)

### Build Verification
- [ ] npm run build 성공
- [ ] npm run lint 통과
</verification>

<success_criteria>
- Dashboard가 Bento Grid Layout — Template 카드 lg:col-span-2
- Landing page가 Bento Grid Layout — Hero 피처 lg:col-span-2, lg:row-span-2
- Responsive: mobile 1 cols → lg 3 cols
- Hover effects 유지
- npm run build 성공
</success_criteria>

<output>
After completion, create `.planning/phases/14-ux/14-02-SUMMARY.md`
</output>