# Phase 17-04: 모바일 Section 구조 재설계

**Goal**: TEMPLATE-STRUCTURE-01, 02, 03 완료 — section 기반 템플릿 구조, 섹션 편집 UI, 실시간 미리보기

**Requirements**:
- TEMPLATE-STRUCTURE-01: 모바일 초대장 Section 구조 (image → announcement → invitation → map → accounts)
- TEMPLATE-STRUCTURE-02: Section 편집 UI (섹션별 내용 입력, 순서 조정 Up/Down 버튼)
- TEMPLATE-STRUCTURE-03: 실시간 미리보기 (편집 중인 내용 즉시 모바일 preview)

---

## Wave 1: Section 기반 템플릿 정의

### Task 1.1: sample.ts에 Section 기반 템플릿 3종 추가

**파일**: `src/data/templates/sample.ts`

**작업 내용**:
- 기존 `WEDDING_TEMPLATE`과 `BIRTHDAY_TEMPLATE`은 유지 (하위 호환)
- 새로운 `ROMANTIC_TEMPLATE`, `CLASSIC_TEMPLATE`, `MODERN_TEMPLATE` 추가
- 각 템플릿은 `sections` 배열을 포함 (5개 섹션: image, announcement, invitation, map, accounts)
- 각 섹션은 고유한 `fields` 정의 포함

**ROMANTIC_TEMPLATE 섹션 구조**:
```
sections: [
  { id: 'image-1', type: 'image', order: 0, label: '메인 이미지', fields: [...] },
  { id: 'announcement-1', type: 'announcement', order: 1, label: '결혼 안내', fields: [...] },
  { id: 'invitation-1', type: 'invitation', order: 2, label: '초대 문구', fields: [...] },
  { id: 'map-1', type: 'map', order: 3, label: '오시는 길', fields: [...] },
  { id: 'accounts-1', type: 'accounts', order: 4, label: '축하 메시지', fields: [...] },
]
```

**각 섹션 필드 정의**:

| Section | 필드 | 타입 | 필수 | 설명 |
|---------|------|------|------|------|
| image | imageUrl | text | false | 이미지 URL |
| image | caption | text | false | 이미지 캡션 |
| announcement | groomName | text | true | 신랑 이름 |
| announcement | brideName | text | true | 신부 이름 |
| announcement | date | text | false | 결혼식 날짜 |
| invitation | groomName | text | true | 신랑 이름 |
| invitation | brideName | text | true | 신부 이름 |
| invitation | groomParents | text | false | 신랑 부모님 |
| invitation | brideParents | text | false | 신부 부모님 |
| invitation | date | text | false | 날짜 |
| invitation | time | text | false | 시간 |
| invitation | venue | text | false | 장소명 |
| invitation | venueAddress | text | false | 주소 |
| map | venue | text | false | 장소명 |
| map | venueAddress | text | false | 주소 |
| accounts | groomBank | text | false | 신랑 측 은행 |
| accounts | groomAccount | text | false | 신랑 측 계좌 |
| accounts | brideBank | text | false | 신부 측 은행 |
| accounts | brideAccount | text | false | 신부 측 계좌 |

**CLASSIC_TEMPLATE**: romantic과 동일한 섹션 구조, 다른 스타일 (SectionStyle)
**MODERN_TEMPLATE**: 동일한 섹션 구조, 다른 스타일

**각 템플릿별 SectionStyle 기본값**:

| Section | romantic | classic | modern |
|---------|----------|---------|--------|
| image | bg:#F9F1EA, accent:#E07A5F | bg:#2D3A32, accent:#C4A882 | bg:#1A1A2E, accent:#E07A5F |
| announcement | bg:#F9F1EA, text:#3D405B | bg:#2D3A32, text:#F4F1DE | bg:#1A1A2E, text:#EAEAEA |
| invitation | bg:#F9F1EA | bg:#F4F1DE | bg:#16213E |
| map | bg:#F9F1EA | bg:#F4F1DE | bg:#16213E |
| accounts | bg:#F9F1EA | bg:#F4F1DE | bg:#16213E |

**검증**:
- TypeScript build pass
- 기존 flat field 템플릿도 정상 렌더링 (하위 호환 확인)

---

## Wave 2: Section 편집 UI

### Task 2.1: TemplateEditor를 Section 기반 편집기로 전환

**파일**: `src/components/templates/editor/TemplateEditor.tsx`

**작업 내용**:
- 기존 flat field 기반 편집기를 Section 기반 편집기로 재구성
- 왼쪽 패널: Section 목록 (드래그 또는 Up/Down 버튼으로 순서 조정)
- 오른쪽 패널: 선택된 Section의 필드 편집기
- Section 선택 시 해당 Section의 필드만 표시

**UI 구조**:
```
┌─────────────────────────────────────────────────┐
│  [템플릿 이름] 편집          [확인] [저장] [⋮]  │
├──────────────────┬──────────────────────────────┤
│ Section 목록     │  선택된 Section 필드 편집     │
│                  │                               │
│ [📷] 메인 이미지  │  필드 1: 입력창              │
│   ↑↓             │  필드 2: 입력창              │
│                  │  필드 3: 입력창              │
│ [💍] 결혼 안내   │                               │
│   ↑↓             │                               │
│                  │                               │
│ [📜] 초대 문구   │                               │
│   ↑↓             │                               │
│                  │                               │
│ [📍] 오시는 길   │                               │
│   ↑↓             │                               │
│                  │                               │
│ [🏦] 축하 메시지  │                               │
│   ↑↓             │                               │
└──────────────────┴──────────────────────────────┘
```

**핵심 기능**:
1. **Section 목록 렌더링**: `template.sections` 기반
2. **Section 선택**: 클릭 시 오른쪽 패널에 해당 Section 필드 표시
3. **Section 순서 조정**: Up/Down 버튼 (dnd-kit 아님, 모바일 친화적)
4. **Section 필드 편집**: 선택된 Section의 `fields` 배열 기반
5. **Section 스타일 설정**: 기존 StyleEditor dialog 연동 (17-03에서 구현)

**SectionItem 컴포넌트**:
```tsx
function SectionItem({
  section,
  isActive,
  onSelect,
  onMoveUp,
  onMoveDown,
  isFirst,
  isLast,
}: {
  section: Section;
  isActive: boolean;
  onSelect: () => void;
  onMoveUp: () => void;
  onMoveDown: () => void;
  isFirst: boolean;
  isLast: boolean;
}) {
  // Section 아이콘 (type별)
  // ↑↓ 버튼 (isFirst/isLast 조건)
  // 클릭 시 onSelect
}
```

**type별 Section 아이콘**:
- `image` → ImageIcon
- `announcement` → Sparkles
- `invitation` → Mail
- `map` → MapPin
- `accounts` → Wallet

### Task 2.2: Section 순서 변경 로직

**작업 내용**:
- `moveSection(index, direction)` 함수 구현
- direction: `'up' | 'down'`
- sections 배열의 order 값 재할당
- React state 업데이트로 리렌더링

```tsx
const moveSection = (index: number, direction: 'up' | 'down') => {
  const newSections = [...template.sections!];
  const temp = newSections[index];
  const targetIndex = direction === 'up' ? index - 1 : index + 1;
  newSections[index] = newSections[targetIndex];
  newSections[targetIndex] = temp;
  // order 재할당
  setTemplate({ ...template, sections: newSections.map((s, i) => ({ ...s, order: i })) });
};
```

### Task 2.3: Section 필드 편집기

**작업 내용**:
- 선택된 Section의 필드만 표시
- 각 필드에 대해 FieldEditor 재사용
- 필드 추가/제거/순서 변경 기능

**SectionFieldEditor 컴포넌트**:
```tsx
function SectionFieldEditor({
  section,
  values,
  onChange,
  onAddField,
  onRemoveField,
  onMoveField,
}: {
  section: Section;
  values: Record<string, string>;
  onChange: (fieldName: string, value: string) => void;
  onAddField: (field: TemplateField) => void;
  onRemoveField: (fieldName: string) => void;
  onMoveField: (index: number, direction: 'up' | 'down') => void;
}) {
  // Section의 fields 기반 렌더링
  // 각 필드에 FieldEditor 사용
}
```

**검증**:
- TypeScript build pass
- Section 클릭 시 해당 필드만 표시
- Up/Down 버튼으로 Section 순서 변경
- 기존 flat field 템플릿도 정상 표시 (sections 없는 경우 fields 기반 fallback)

---

## Wave 3: 실시간 미리보기

### Task 3.1: TemplateEditor에서 실시간 preview 연동

**파일**: `src/components/templates/editor/TemplateEditor.tsx`

**작업 내용**:
- editor 상태 변경 시 즉시 preview 반영
- TemplateEngine에 현재 편집 데이터 전달
- sections 기반 렌더링으로 preview

**구현 방식**:
1. `TemplateEditor` 내부에 `previewData` state 관리
2. 필드 변경 시 `previewData` 즉시 업데이트
3. preview 영역에 `TemplateEngine`에 `previewData` 전달
4. debounce 없이 즉시 반영 (실시간성 확보)

```tsx
// editor 내부에 preview data state
const [previewData, setPreviewData] = useState<Record<string, string>>({});

// 필드 업데이트 시 preview도 함께 업데이트
const updateField = (fieldName: string, value: string) => {
  // 기존 로직...
  setPreviewData(prev => ({ ...prev, [fieldName]: value }));
};
```

### Task 3.2: 모바일 미리보기 모드

**작업 내용**:
- editor 하단에 모바일/데스크톱 토글 버튼 추가
- 모바일 모드: 375px 너비로 preview
- 섹션별 애니메이션 미리보기

**UI**:
```tsx
<div className="flex items-center gap-2 mt-4">
  <Button variant={previewMode === 'desktop' ? 'default' : 'outline'} size="sm">
    <Monitor className="w-4 h-4" /> 데스크톱
  </Button>
  <Button variant={previewMode === 'mobile' ? 'default' : 'outline'} size="sm">
    <Smartphone className="w-4 h-4" /> 모바일
  </Button>
</div>
```

### Task 3.3: edit 페이지 통합

**파일**: `src/app/(main)/templates/[id]/edit/page.tsx`

**작업 내용**:
- 기존 edit 페이지를 Section 기반 편집기로 전환
- TemplateEditor 컴포넌트 사용
- sections 데이터 로드/저장 지원
- API 호출 시 `sections` 필드 포함

**변경 사항**:
1. `fields` state → `sections` state 추가
2. API 응답에서 `sections` 파싱
3. 저장 시 `sections` 전송
4. preview에 TemplateEngine 사용 (sections 기반)

**API 호출 변경**:
```tsx
// GET 응답 파싱
const data: Template = await response.json();
setSections(data.sections || []);

// PATCH 요청
body: JSON.stringify({
  name, category, thumbnail, layout,
  sections,  // NEW
  fields,    // legacy (하위 호환)
  is_published: isPublished,
})
```

**검증**:
- TypeScript build pass
- 필드 편집 시 preview 즉시 반영
- 모바일/데스크톱 토글 정상 동작
- edit 페이지에서 sections 저장/로드

---

## Wave 4: 통합 검증

### Task 4.1: TemplateLibrary에 Section 기반 템플릿 표시

**파일**: `src/app/(main)/templates/page.tsx`

**작업 내용**:
- Section 기반 템플릿 3종 (romantic, classic, modern)을 템플릿 라이브러리에 추가
- 기존 WEDDING_TEMPLATE, BIRTHDAY_TEMPLATE도 유지

**변경 사항**:
1. `templates/page.tsx`에서 Section 기반 템플릿 import
2. 템플릿 목록에 5종 표시 (romantic, classic, modern, wedding, birthday)
3. Section 기반 템플릿 클릭 → preview modal (17-02에서 구현)

### Task 4.2: TemplateEngine 섹션별 스타일 적용 강화

**파일**: `src/components/templates/engine/TemplateEngine.tsx`

**작업 내용**:
- SectionStyle의 animation, backgroundColor, textColor, fontFamily 적용 확인
- animationDelay 적용 추가
- 섹션별 스타일이 preview와 실제 렌더링에서 동일하게 적용

**검증**:
- 각 섹션별 스타일 적용 확인
- animation이 섹션별로 다르게 적용
- backgroundColor, textColor이 섹션별로 적용

### Task 4.3: E2E 시나리오 테스트

**테스트 시나리오**:
1. 템플릿 라이브러리에서 romantic 클릭 → preview modal 열림
2. "편집하기" 클릭 → edit 페이지로 이동, Section 목록 표시
3. "결혼 안내" Section 클릭 → 신랑/신부 이름 필드 표시
4. 신랑 이름 변경 → preview 즉시 반영
5. "오시는 길" Section Up 버튼 → "초대 문구"와 순서 변경
6. 모바일 토글 → 375px preview
7. 저장 → sections 데이터가 API로 전송

**검증**:
- 모든 시나리오 정상 동작
- TypeScript build pass
- ESLint 에러 없음

---

## Files to Modify

| File | Change | Description |
|------|--------|-------------|
| `src/data/templates/sample.ts` | **Major rewrite** | Section 기반 템플릿 3종 추가 |
| `src/components/templates/editor/TemplateEditor.tsx` | **Major rewrite** | Section 기반 편집기로 전환 |
| `src/components/templates/editor/FieldEditor.tsx` | Minor | Section 필드 편집용 헬퍼 추가 |
| `src/components/templates/engine/TemplateEngine.tsx` | Minor | animationDelay 적용 추가 |
| `src/app/(main)/templates/[id]/edit/page.tsx` | **Major rewrite** | Section 기반 편집기로 전환 |
| `src/app/(main)/templates/page.tsx` | Minor | Section 기반 템플릿 추가 |
| `src/hooks/use-template-editor.ts` | Minor | Section 관련 헬퍼 추가 |

---

## Risks & Mitigations

| Risk | Impact | Mitigation |
|------|--------|------------|
| 기존 flat field 템플릿 호환성 | 높음 | TemplateEngine에서 sections 없으면 flat field fallback 유지 |
| edit 페이지 API 변경 | 높음 | API는 sections + fields 둘 다 받도록 설계 |
| 실시간 preview 성능 | 중음 | 큰 이미지 있을 시 debounce 적용 고려 |
| Section 필드 추가/제거 | 중음 | TemplateField 타입 확장 없이 기존 fields 배열 사용 |

---

## Success Criteria

- [x] TEMPLATE-STRUCTURE-01: 5개 Section (image, announcement, invitation, map, accounts) 기반 템플릿 3종 구현
- [x] TEMPLATE-STRUCTURE-02: Section 편집 UI (Up/Down 버튼 순서 조정, 필드 편집) 구현
- [x] TEMPLATE-STRUCTURE-03: 실시간 미리보기 (편집 즉시 반영, 모바일/데스크톱 토글) 구현
- [x] 기존 flat field 템플릿 하위 호환 유지
- [x] TypeScript build pass
- [x] ESLint 에러 없음

## 완료 일시

2026-05-01

## 검증 결과 (Post-Implementation Review)

### 1. TemplatePreview.tsx split-view 렌더링
- **결과**: ✅ 정상 동작
- split-view 모드에서 `TemplateEngine`을 통해 section 기반 템플릿 렌더링
- section 기반 여부와 무관하게 동일하게 동작

### 2. SECTION_BASED_TEMPLATES API 연동
- **결과**: ✅ 완료 (이전 검증 결과와 달리 API 연동은 실제로 완료됨)
- `src/app/api/templates/route.ts`: SECTION_BASED_TEMPLATES import 및 병합
- `src/app/api/templates/[id]/route.ts`: findSectionBasedTemplate() 폴백 조회
- POST/PATCH 모두 sections 지원

### 3. Build 검증
- **결과**: ✅ TypeScript + Next.js 빌드 성공
- TemplateEditor.tsx rules-of-hooks 오류 수정 완료
- toss.test.ts reserved word 오류 수정 완료

### 4. 수정된 파일
- `src/components/templates/editor/TemplateEditor.tsx` — `useState` hooks 순서 수정
- `src/app/api/templates/route.ts` — SECTION_BASED_TEMPLATES import 및 병합
- `src/app/api/templates/[id]/route.ts` — sample.ts 폴백 조회, PATCH sections 지원
- `src/lib/payment/toss.test.ts` — `module` → `mod` 변수명 변경

### 5. Plan 미이행 항목 (새로 발견)
- **결과**: ❌ 4건 미이행
- `templates/page.tsx`: SECTION_BASED_TEMPLATES 표시 안됨
- `TemplateEngine.tsx`: animationDelay 적용 안됨
- `use-template-editor.ts`: Section 관련 헬퍼 미추가
- `FieldEditor.tsx`: Section 필드 편집기 미추가

### 6. E2E 테스트
- **결과**: ❌ 6건 실패 (33건 통과)
- template.e2e.ts 4건: mock 구조 불일치
- profile.e2e.ts 1건 + template.e2e.ts 1건: viewport visibility

## 다음 작업

Phase 17-04 완료 후 진행 (우선순위 순):
1. **E2E 테스트 6건 수정** (최우선) — mock 구조 불일치 + viewport visibility
2. **`templates/page.tsx`에 SECTION_BASED_TEMPLATES 표시**
3. **`landing/page.tsx:223` JSX entities 수정**
4. (선택사항) `TemplateEngine.tsx` animationDelay, `use-template-editor.ts` Section 헬퍼, `FieldEditor.tsx` Section 편집기
